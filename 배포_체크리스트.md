# 경영 대시보드 배포 체크리스트

이 문서는 대표이사 납품용 Windows 서버 PC에서 실행한다. 대시보드 포트는 LAN이나 인터넷에 직접 열지 않고, `127.0.0.1:8080`을 Tailscale Serve로만 전달한다.

## 1. 운영 전제

- Python 3.11 이상과 Tailscale 최신 버전을 설치한다.
- 씽크와이즈 DB에 네트워크로 접근할 수 있고 계속 켜 둘 Windows 서버 PC에 대시보드와 `thinkwise-wiki` 저장소를 함께 둔다. MariaDB 자체가 같은 PC에 설치될 필요는 없다.
- 위키의 증분 동기화 작업만 씽크와이즈 `work_log`를 조회하고, 두 화면은 같은 `wiki_index.db`를 읽는다.
- 씽크와이즈 계정은 SELECT 전용으로 유지한다.
- `.env`, `data/dashboard.db`, 위키의 `data/wiki_index.db`는 실행 계정과 관리자만 읽도록 Windows ACL을 제한한다.

권장 폴더 구조는 아래와 같다. 설치 드라이브와 상위 폴더는 서버 환경에 맞게 정하고, 두 저장소 이름만 유지하면 기본 스크립트가 위키 위치를 자동으로 찾는다. 서로 다른 폴더에 둘 때는 스크립트의 `-WikiRoot` 인자로 실제 경로를 넘긴다.

```text
<설치 루트>\
├── yjs_backoffice\
└── thinkwise-wiki\
```

공유 수집 흐름은 다음과 같다.

```text
씽크와이즈 MariaDB work_log
        │  PK(indx) 증분 SELECT 1회
        ▼
thinkwise-wiki/data/wiki_index.db
        ├── 위키 검색
        └── 경영 대시보드의 최근 변경·프로젝트 최종 활동
```

대시보드는 협업방 기본정보와 현재 접속자처럼 공유 색인에 없는 소량 데이터만 MariaDB에서 별도로 SELECT한다.

## 2. 환경 준비

먼저 두 저장소가 들어 있는 상위 폴더를 `$InstallRoot`에 한 번만 지정한다. 아래 명령은 설치 드라이브나 Windows 사용자명이 달라도 그대로 사용할 수 있다. 대시보드 가상환경을 만든 뒤 의존성을 설치하며, 공유 색인 동기화도 이 가상환경을 사용해 운영 Python 환경을 하나로 유지한다.

```powershell
$InstallRoot = "C:\YJS"
$DashboardRoot = Join-Path $InstallRoot "yjs_backoffice"
$WikiRoot = Join-Path $InstallRoot "thinkwise-wiki"

Set-Location -LiteralPath $DashboardRoot
python -m venv .venv
$PythonPath = Join-Path $DashboardRoot ".venv\Scripts\python.exe"
& $PythonPath -m pip install `
  -r (Join-Path $DashboardRoot "requirements.txt") `
  -r (Join-Path $WikiRoot "requirements.txt")
if (-not (Test-Path -LiteralPath .env -PathType Leaf)) {
  Copy-Item .env.example .env
}
```

이후 명령은 같은 관리자 PowerShell 창에서 이어 실행한다. 새 창을 열었다면 위의 `$InstallRoot`, `$DashboardRoot`, `$WikiRoot` 세 줄을 먼저 다시 실행한다.

대시보드 `.env`에는 다음 운영값을 설정한다. 실제 사용자명·비밀번호는 문서나 Git에 기록하지 않는다.

```dotenv
APP_ENV=production
APP_HOST=127.0.0.1
APP_PORT=8080
APP_DEMO_MODE=false
APP_TRUST_TAILSCALE_HEADERS=true
APP_ALLOWED_TAILSCALE_USER=<대표이사 Tailscale 로그인>

DB_HOST=<씽크와이즈 MariaDB 주소>
DB_PORT=3306
DB_USER=<씽크와이즈 SELECT 전용 계정>
DB_PASSWORD=<비밀값>
THINKWISE_INDEX_PATH=..\thinkwise-wiki\data\wiki_index.db
```

메일 계정은 준비된 계정만 `MAIL_*_ENABLED=true`로 바꾸고 사용자·앱 비밀번호와 `https://`로 시작하는 해당 웹메일 받은편지함 URL을 입력한다. 다우오피스 사용자는 아이디만이 아니라 전체 메일 주소를 입력한다. 계정이 준비되지 않은 공급자는 `false`로 두면 다른 데이터와 준비된 메일은 정상 작동한다.

위키 `.env`에도 같은 씽크와이즈 SELECT 전용 접속값을 설정한다. 권장 폴더 구조에서는 위키의 `INDEX_DB_PATH`를 비워 기본값 `data/wiki_index.db`를 사용한다. 경로를 따로 지정했다면 대시보드의 `THINKWISE_INDEX_PATH`와 반드시 같은 파일을 가리켜야 한다. 자동시작 등록과 최종 감사 스크립트가 두 경로의 일치를 검사하며, 비밀값은 출력하지 않는다.

관리자 PowerShell에서 비밀값과 SQLite 폴더의 상속 권한을 제거하고 SYSTEM·관리자·현재 배포 사용자만 접근하도록 제한한다. 스크립트는 두 데이터 폴더 안의 기존 DB·백업에도 같은 ACL을 재귀 적용하며, 이후 생성되는 WAL·백업은 제한된 부모 권한을 상속한다. 자동시작 실행 계정을 SYSTEM 이외로 바꿀 경우에는 이 스크립트를 그대로 실행하지 말고 해당 계정 권한을 먼저 설계한다.

```powershell
Set-Location -LiteralPath $DashboardRoot
.\scripts\secure_runtime_acl.ps1 -WikiRoot $WikiRoot
```

## 3. 공유 색인과 로컬 앱 검증

위키 폴더에서 최초 전체 색인을 한 번 만들고, 이후에는 증분 동기화만 사용한다.

```powershell
Push-Location -LiteralPath $WikiRoot
& (Join-Path $DashboardRoot ".venv\Scripts\python.exe") -m app.sync --full
& (Join-Path $DashboardRoot ".venv\Scripts\python.exe") -m app.sync
Pop-Location
```

대시보드를 직접 실행해 실데이터와 보안 설정을 확인한다.

```powershell
Set-Location -LiteralPath $DashboardRoot
.\scripts\run_dashboard.ps1
```

다른 관리자 PowerShell에서 위의 경로 변수 세 줄을 다시 설정하고 다음 검증을 실행한다. 로그인 값은 콘솔 기록 정책에 맞게 입력한다.

```powershell
Set-Location -LiteralPath $DashboardRoot
$AllowedTailscaleUser = Read-Host "Allowed Tailscale login"
.\scripts\verify_delivery.ps1 `
  -AllowedTailscaleUser $AllowedTailscaleUser `
  -RequireProduction
Remove-Variable AllowedTailscaleUser
```

검증은 localhost 단독 바인딩, `Cache-Control: no-store`, 실데이터 모드, 공유 색인 신선도, 프로젝트 1건 이상을 확인한다.

## 4. 재부팅 자동 시작

관리자 PowerShell에서 두 작업을 등록한다.

```powershell
.\scripts\install_startup_tasks.ps1 -WikiRoot $WikiRoot -StartNow
Get-ScheduledTask -TaskName "YJS*" | Select-Object TaskName, State
```

- `YJS ThinkWise Shared Index Sync`: 위키 증분 색인을 60초마다 한 번 갱신한다.
- `YJS Management Dashboard`: uvicorn 단일 프로세스를 `127.0.0.1:8080`에서 실행한다.
- 두 작업은 `SYSTEM`, 부팅 시 시작, 실패 시 1분 뒤 재시작, 중복 실행 금지로 등록된다.

등록 후 서버를 한 번 재부팅하고 작업 상태와 `verify_delivery.ps1`을 다시 확인한다.

## 5. Tailscale Serve와 대표이사 전용 권한

서버와 대표이사 기기에 Tailscale을 설치하고 같은 tailnet에 로그인한다. 서버의 대시보드가 실행 중일 때 다음 스크립트로 장치 이름을 비민감 기본값 `yj-dashboard`로 바꾼 뒤 tailnet 전용 HTTPS 프록시를 영구 설정한다. 다른 이름을 쓰려면 회사명·직원명·업무명이 드러나지 않는 DNS 형식 이름을 `-DeviceName`으로 지정한다.

```powershell
.\scripts\configure_tailscale_serve.ps1
tailscale serve status
```

스크립트는 공식 `tailscale set --hostname=<name>` 구문으로 장치 이름을 먼저 확정한다. 이 이름은 MagicDNS와 HTTPS 주소에 사용되므로 인증서 설정 뒤 민감한 이름을 발견하는 상황을 피한다. Serve는 `--bg`로 설정되어 재부팅과 `tailscale down/up` 뒤에도 재개된다. **`tailscale funnel`과 공유기 포트포워딩은 사용하지 않는다.**

Tailscale 관리자 콘솔에서 서버와 대표이사 PC·휴대폰을 승인한다. 서버에는 예를 들어 `tag:management-dashboard` 태그를 부여하고, 대표이사 기기의 Tailscale IP를 `hosts` 별칭으로 고정해 **허용된 두 기기만** 서버의 TCP 443에 접근하게 Grants를 구성한다. 애플리케이션의 `APP_ALLOWED_TAILSCALE_USER` 검증이 이 장치 제한에 사용자 계정 검증을 한 번 더 겹친다.

```json
{
  "hosts": {
    "ceo-pc": "<대표이사 PC의 Tailscale IP>",
    "ceo-phone": "<대표이사 휴대폰의 Tailscale IP>"
  },
  "tagOwners": {
    "tag:management-dashboard": []
  },
  "grants": [
    {
      "src": ["ceo-pc", "ceo-phone"],
      "dst": ["tag:management-dashboard"],
      "ip": ["tcp:443"]
    }
  ]
}
```

Grants는 허용 규칙을 합산한다. 기본 `src: ["*"]` 또는 `autogroup:member`에서 해당 서버·포트를 허용하는 기존 규칙이 남아 있으면 위 규칙만 추가해도 대표이사 전용이 되지 않는다. 관리자 콘솔의 정책 검사 기능으로 두 허용 기기는 통과하고, 같은 대표이사 계정의 미등록 기기와 일반 계정 기기는 모두 거부되는지 확인한다.

## 6. 최종 인수 시험

먼저 관리자 PowerShell에서 읽기 전용 자동 감사를 실행한다. 비밀값과 메일 내용은 출력하지 않으며, 실패한 항목 이름만 표시한다.

```powershell
.\scripts\audit_delivery_readiness.ps1 -WikiRoot $WikiRoot
```

Serve 설정에서 다른 비민감 장치 이름을 선택했다면 감사에도 같은 `-DeviceName` 값을 전달한다.

아래 항목을 모두 실제 기기에서 확인한 뒤 납품 완료로 처리한다.

- 서버 재부팅 후 3분 이내에 두 예약 작업이 실행 상태가 된다.
- 서버에서 `verify_delivery.ps1 -RequireProduction`이 통과한다.
- 대표이사 PC에서 HTTPS 주소가 열리고 프로젝트·최근 활동이 표시된다.
- 대표이사 휴대폰에서 Wi-Fi를 끈 외부망에서도 같은 HTTPS 주소가 열린다.
- 허용한 대표이사 PC·휴대폰만 접속되고, 같은 계정의 미등록 기기와 다른 계정 기기에서는 모두 거부된다.
- `tailscale serve status`에는 localhost 프록시만 있고 Funnel은 없다.
- `tailscale status`의 서버 장치 이름이 `yj-dashboard` 또는 승인한 비민감 이름이다.
- 수동 상태를 바꾸고 새로고침·재부팅 후에도 값이 유지된다.
- `data/backups/dashboard-YYYY-MM-DD.db`를 임시 SQLite로 열어 `project_mark`와 `setting`을 조회할 수 있다.
- 준비한 메일 계정별 안 읽은 수와 최근 헤더가 실제 웹메일과 일치하고, 대시보드 조회 뒤에도 메일의 안 읽음 상태가 유지된다.

Tailscale CLI와 Grants의 장치 `hosts` 원본 선택자 형식은 2026-08-18 기준 공식 문서의 현재 구문을 사용했다: [장치 이름 변경](https://tailscale.com/kb/1098/machine-names), [Serve](https://tailscale.com/docs/features/tailscale-serve), [Serve CLI](https://tailscale.com/docs/reference/tailscale-cli/serve), [Grants 구문](https://tailscale.com/docs/reference/syntax/grants).

## 7. 장애 시 확인 순서

1. `Get-ScheduledTask -TaskName "YJS*"`와 작업 스케줄러의 최근 실행 결과를 확인한다.
2. `tailscale status`, `tailscale serve status`를 확인한다.
3. `scripts\verify_delivery.ps1`로 localhost·API·공유 색인 상태를 확인한다.
4. 위키 `python -m app.sync`를 한 번 직접 실행해 증분 색인 오류를 확인한다.
5. 메일만 실패하면 해당 공급자만 `MAIL_*_ENABLED=false`로 내려 나머지 대시보드를 유지한다.

비밀값, 직원 이름, 메일 제목은 장애 기록이나 화면 캡처에 남기지 않는다.
