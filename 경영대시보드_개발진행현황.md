# 경영 대시보드 개발 진행 현황

> 이 문서는 개발 진행의 정본입니다. 코드·설정·테스트·문서를 변경하는 모든 작업은 종료 전에 이 문서를 함께 갱신합니다.

- **마지막 갱신:** 2026-09-02 11:04 KST
- **현재 단계:** 회사 공유 Drive 전용 조회와 Google(기본)·씽크와이즈 전환 탭을 원격·운영 서버에 반영하고 실제 API·운영 납품 검증·비공개 HTTPS·tailnet only를 모두 확인했다. 사용자가 운영 화면을 확인해 현재 상태로 대표이사 보고 가능하다고 승인했으므로 공용 할 일과 Google 일정·공유 Drive 요구 보강을 완료 처리하고 세션 마감
- **구현 진행:** 3 / 5 Phase 완료 (Phase 1, Phase 2, Phase 3)
- **다음 작업:** 대표이사 보고와 피드백 반영 → Gmail·Naver·Grants·재부팅 운영 검증
- **차단 사항:** NAS 경로는 **철회**했다. 근거 두 가지 — 대표이사 요구 정본(`목표.html`)에 작업자 표시가 없고, NAS 로그인 계정이 전원 공용 단일 ID라 사용자명으로 작업자를 식별할 수 없다(작업자를 알려면 IP가 담긴 로그가 필요한데 그 경로는 확보하지 못했다). UDP 활동 수신기와 File Station 파일럿은 커밋된 적이 없어 되돌리기로 함께 소멸했고, 필요하면 처음부터 다시 만들어야 한다. 자동 브라우저 검증 환경은 없었지만 사용자가 운영 화면을 확인하고 대표이사 보고 가능 상태로 승인했다. Gmail·Naver, 초대 계정 실제 브라우저·최종 Grants·서버 재부팅 검증은 기존대로 대기

## 한눈에 보는 진행 상태

| 단계 | 상태 | 완료 기준 | 현재 결과 |
|---|---|---|---|
| 기획·화면 설계 | 완료 | 범위·보안·상태 규칙·목업 승인 | 차트 적용 목업 승인 |
| 요구 보강 · 할 일 | 완료 | 대표이사 공용 목록의 추가·재접속 유지·완료/삭제와 실제 화면 검증 | SQLite 영속화·30일 일일 백업·허용 계정 간 공용 범위·입력 검증·동일 출처 보호·완료 시 즉시 제거를 구현. 운영 서버 재시작 후 비공개 HTTPS에서 추가·조회·삭제 수직 슬라이스를 통과했고 사용자가 현재 운영 화면을 보고용으로 승인 |
| 요구 보강 · Google 일정·Drive | 완료 | 대표이사 본인 계정의 이번 주 일정·회사 공유 Drive 최근 수정 파일을 읽기 전용으로 표시하고 실제 OAuth·화면 검증 | 실제 OAuth 정상화 뒤 회사 공유 Drive ID를 지정하는 전용 조회와 Google 기본·씽크와이즈 전환 탭을 운영 반영. 실제 API `drive_scope=shared`·일정 5건·공유 파일 8건·stale=false, 운영 검증·HTTPS 200을 통과했고 사용자가 현재 운영 화면을 보고용으로 승인 |
| Phase 0-1 · 최종 사전 검증 | 완료 | 필드 의미·중복·증분키 검증 | 서버 재검증에서 `work_log.indx` PK·접속 중복 확인. `UPD_DATE`는 87건 중 12건만 실제 최종 활동과 5분 이내 일치해 공유 작업 이력 색인을 유지. 87건 모두 `MEMBER_ID=REG_ID`, 참여자 일치는 2건이라 `MEMBER_NAME`을 개설자로 확정 |
| Phase 1 · 백엔드·캐시 | 완료 | 실데이터 `/api/dashboard`와 상태 점검 통과 | 최신 공유 색인과 SELECT 전용 DB로 API 200, 상태 `ok`, 87개 프로젝트·개설자 87건, 최근 변경 30건, 1분 활동 갱신, stale=false 확인 |
| Phase 2 · 화면 연결 | 완료 | 승인 목업에 실데이터 표시 | API·상태·설정·수동 표시·메일 UI와 오류 상태를 연결하고 사용자 PC 표시 확인 완료. 후속 여백 개선으로 메일 카드를 전체 폭 2열에 배치하고 최근 메일을 최대 5건에서 10건으로 확대했으며 실서버 사용자 재확인 대기 |
| Phase 3 · 상태·설정 | 완료 | 완료·진행·보류·취소와 설정 저장 검증 | 별도 SQLite 저장·취소·임계값 변경·상태와 설정 감사 시각·구버전 자동 마이그레이션·일일 백업·정확히 30일 보관·복원 시험 통과 |
| Phase 4 · 메일 | 검증 중 | 3계정 안 읽은 메일 통합·읽음 상태 보존 | 서버에서 다우오피스 단일 활성 계정의 신선한 스냅샷까지 운영 검증 통과. Gmail·Naver는 자격증명 재확인 전까지 비활성화했으며 추후 재검증 필요 |
| Phase 5 · 상주화·외부 접속 | 검증 중 | 재부팅 자동 시작·비공개 HTTPS 외부 접속 검증 | Tailscale·localhost 전용 대시보드·SYSTEM 자동시작 2작업·ACL 적용 완료. 유지보수 담당자·대표이사·초대 외부 계정까지 3개 계정의 서버 인증과 유지보수 담당자 비공개 HTTPS 200 확인. 초대 수락·대표이사 기기 실제 브라우저·Grants·비허용 사용자 거부 시험 대기, 재부팅 검증은 새벽으로 연기 |
| 추가 기획 · NAS 활동 | 철회 | — | 대표이사 요구 정본에 작업자 표시가 없고 NAS 계정이 전원 공용 단일 ID라 작업자 식별이 불가능해 철회. UDP 수신기·파서·API·화면 카드와 File Station 파일럿(최근 파일·다운로드·불투명 문서 ID)을 모두 제거했다. 철회 전 실측으로 확인된 한계 두 가지를 기록으로 남긴다 — Synology BSD 로그는 과거형 동사(`deleted`·`uploaded`)를 써서 작업 종류가 항상 `unknown`이 되고, `from [IP]`가 경로 후보로 잡혀 진짜 경로까지 함께 폐기되어 폴더·파일명이 비었다 |

상태는 `대기` → `진행 중` → `검증 중` → `완료` 순서로 변경합니다. 실제 검증을 통과하기 전에는 `완료`로 표시하지 않습니다.

## 현재 작업과 다음 작업

### 현재

- Google Calendar 이번 주 월~일 일정은 대표이사 계정에서, 최근 수정 파일 8건은 설정된 회사 공유 Drive에서 읽는 최소 범위 OAuth 수직 슬라이스 구현
- 긴 단일 화면을 Google 기본 탭과 씽크와이즈 탭으로 분리하고 마우스·키보드 전환, 선택 상태, 숨은 패널과 씽크와이즈 설정 버튼 표시를 동기화
- OAuth state는 SQLite에 원문 대신 SHA-256 해시로 10분간 저장하고 콜백에서 원자적으로 1회 소비하며, 갱신 토큰은 기존 Windows ACL을 보존한 운영 `.env`에만 무출력 기록
- Google 수집을 기존 대시보드와 분리한 5분 캐시로 운영하고 실패 시 마지막 성공 스냅샷을 보존하면서 Google 영역만 degraded 처리
- 요구 정본 순서대로 일정·Drive·할 일을 720px 상단 영역에 배치하고 480px에서 통계 타일을 2열로 전환; Drive 링크는 `https://drive.google.com`만 새 탭으로 허용
- Google 비활성·연결 필요·로딩·빈 목록·실패·마지막 갱신 상태와 화면의 `Google 계정 연결` 왕복을 구현
- 대표이사 전용이라는 최초 기획에 따라 허용된 Tailscale 계정이 하나의 `todo_item` 목록을 함께 관리하며 계정별 소유자 컬럼은 두지 않음
- 완료 이력을 쌓아 유지보수 부담을 만들지 않도록 체크 완료와 `✕` 삭제가 모두 SQLite 행을 즉시 제거
- `GET /api/todos`·`POST /api/todos`·`DELETE /api/todos/{id}`와 공백 제거·1~120자 검증, 기존 동일 출처 요청 보호·`no-store`·Tailscale 인증을 재사용
- `목표.html`의 720px 카드·입력창·체크박스·빈 상태를 현재 경영 화면 상단에 적용하고 모든 동적 문구를 `textContent`로 출력
- SQLite 변경 때마다 기존 당일 백업을 갱신하고 재시작 후 목록 복원·완료 삭제가 백업에도 반영되는지 검증
- Windows 서버에 전용 읽기 전용 GitHub 배포 키와 게시 브랜치의 복수 사용자 구현 커밋 `4f3e183`을 포함해 배포하고 전용 가상환경·운영 `.env`·ACL 구성
- 기존 위키 웹 서비스는 유지하고 기존 10분 동기화 작업은 비활성화한 뒤, 새 60초 공유 색인 동기화와 대시보드를 SYSTEM 시작 작업으로 전환
- 서버 실데이터 API에서 프로젝트 87건·공유 색인·인증 없는 요청 403·오늘자 SQLite 백업 읽기 전용 조회 확인
- Tailscale 설치·tailnet 연결·비민감 장치 이름·tailnet 전용 Serve 적용 완료, HTTPS 200·보안 헤더·Funnel 비공개와 실제 허용 사용자 Windows PC 화면 표시 확인
- 먼저 확인할 프로젝트 아래 여백을 제거하기 위해 최근 안 읽은 메일 카드를 전체 폭 2열로 재배치하고 표시 상한을 10건으로 확대
- 실메일 분리 검사에서 다우오피스 성공, Gmail·Naver 실패 확인
- 위키 증분 수집기가 `work_log`를 한 번만 공유 SQLite 색인으로 가져오고 위키와 대시보드가 함께 읽는 구조 구현
- FastAPI 실데이터 API, 1분 씽크와이즈·5분 메일 독립 캐시와 실패 시 이전 데이터 유지 구현
- 승인 목업에 상태·설정·메일·원본 신선도 표시와 60초 화면 갱신 연결
- 대시보드 전용 SQLite 수동 상태·설정·30일 백업과 복원 시험 구현
- 메일 3계정 IMAP 헤더 전용 수집, 계정별 장애 격리, 안 읽음 보존 구현
- Windows 부팅 자동시작, 공유 색인 단일 동기화 작업, Tailscale Serve와 ACL·납품 준비 자동 감사 스크립트 작성
- 서버 PC의 새 Codex가 현재 맥락을 재탐색하지 않고 배포할 수 있도록 목표·아키텍처·코드 전달 위험·명령·중단 조건·완료 기준을 한 문서로 정리
- 이후 유지보수에서 로컬 검증·push·SSH 서버 pull·변경별 재시작·운영 검증·안전한 복구를 바로 실행할 수 있는 실제 서버 배포 런북 작성
- 유지보수 담당자와 대표이사가 각자 Tailscale 계정으로 접속할 수 있도록 쉼표 구분 복수 허용 목록, 기존 단일 설정 호환, 전체 허용 계정 자동 검증을 구현·서버 반영하고 대시보드 작업 재시작·운영 검증 통과
- 초대된 외부 Gmail 기반 Tailscale 계정을 세 번째 허용값으로 추가하고 ACL 재적용·대시보드 재시작·세 계정 운영 인증 검증 통과

### 다음

1. 현재 운영 화면을 대표이사에게 보고하고 피드백이 있으면 요구 정본과 진행 현황에 반영한다
2. 초대된 외부 계정의 초대 수락·기기 로그인을 완료하고 대표이사 기기에서 비공개 HTTPS 주소를 열어 실제 화면 접속 확인
3. 대표이사·유지보수 담당자의 허용 PC·휴대폰을 정한 뒤 Grants를 적용하고 허용 기기 성공·비허용 사용자 거부 확인
4. Gmail·Naver IMAP/앱 비밀번호 설정을 보정해 세 계정의 실제 안 읽음 수와 읽음 상태 보존 확인
5. 전체 납품 감사를 다시 실행하고 사용자 요청 시간에 서버를 재부팅해 공유 색인·대시보드 자동 복구 확인

### 다음 세션 시작점

- 서버 배포 경로는 `C:\apps\yjs_backoffice`, 공유 위키 경로는 `C:\apps\thinkwise-wiki`이며 비밀값은 각 `.env`에만 있습니다.
- `YJS Management Dashboard`와 `YJS ThinkWise Shared Index Sync`는 SYSTEM으로 실행 중이고 기존 `ThinkwiseWikiSync`는 중복 수집 방지를 위해 비활성화했습니다. 기존 `ThinkwiseWiki` 웹 작업은 유지했습니다.
- Tailscale Serve는 서버의 `127.0.0.1:8080`을 tailnet 내부 HTTPS로만 프록시하며 공개 Funnel과 LAN에는 노출하지 않았습니다.
- 공용 할 일 `e90645a`, Google Calendar·Drive `8a9ff90`, PKCE 수정 `021153a`, 회사 공유 Drive·탭 `9bbdadf`, 안전한 공유 Drive 설정 `df86342`가 운영 서버에 반영됐습니다. 실제 OAuth·공유 scope·운영 검증·대시보드 재시작까지 통과했고 사용자가 대표이사 보고 가능 상태로 승인했습니다.
- Gmail·Naver 실메일, Grants·비허용 사용자 거부·재부팅 시험은 통과 전 Phase 4·5를 완료로 바꾸지 않습니다.

## 준비가 필요한 외부 입력

| 항목 | 필요한 시점 | 상태 |
|---|---|---|
| 최신 `yjs_backoffice` 작업 트리 전달 | 서버 배포 시작 전 | 완료 — 원격 `agent/management-dashboard-delivery`의 `f42b603`을 서버에 clone하고 전체 49개 테스트 통과 |
| 납품 Windows 서버 PC 접속·관리자 권한 | Phase 5 | 완료 — 관리자 SSH 접속, 가상환경·ACL·SYSTEM 시작 작업 구성 완료 |
| 다우오피스·Gmail·네이버 접속 정보 | Phase 4 | 일부 완료 — 다우오피스 성공, Gmail·Naver IMAP 실패로 설정 보정 필요 |
| Google Cloud Web OAuth Client ID·Secret·Redirect URI와 대표이사 1회 동의 | Google 일정·Drive 운영 연결 | 완료 — 운영 `.env` 입력, PKCE 보완 뒤 대표이사 동의, 회사 공유 Drive ID 반영과 실제 Calendar·공유 Drive 표시·운영 검증 완료 |
| Tailscale Serve 관리자 활성화·허용 기기 목록 | Phase 5 | 진행 중 — 관리자 활성화·tailnet 전용 HTTPS·현재 허용 사용자 PC 접속 완료, 대표이사 계정·기기·Grants 확정 필요 |
| 일정 관리 도구 사용 현황 | 추후 일정 섹션 검토 시 | 보류 |
| NAS 활동 화면 정책 | UDP 전환 기획 | 완료 — 별도 탭 없이 경영 현황에서 씽크와이즈 최근 변경과 나란히 표시하는 의미 추론 없는 참고용 활동 피드로 승인 |
| NAS 로그 송신 IPv4·대상 루트 | 운영 서버 반영 전 | 사용자 승인·값 확인 필요 — 비밀값과 원문 출력 없이 `NAS_LOG_*`에 설정 |
| Windows 방화벽 UDP 514 제한 | 운영 서버 반영 전 | 미실행 — 실제 NAS IPv4만 인바운드 허용하고 광범위 공개 금지 |
| 기존 File Station 계정 최소 권한 | 레거시 파일 API 재사용 시 | 미충족 이력 유지 — 현재 UI는 파일 API를 호출하지 않으며 재사용 전 전용 공유 폴더·ACL 재검토 필요 |

비밀번호·앱 비밀번호·토큰·메일 주소 등 실제 비밀값은 이 문서에 기록하지 않습니다.

## 확정된 핵심 결정

- `work_log`는 위키 증분 수집기가 PK 커서로 한 번만 조회해 공유 SQLite 색인을 만들고, 위키와 대시보드가 각 용도에 맞게 파생 조회합니다.
- 공유 색인이 없거나 3분 넘게 갱신되지 않으면 조용히 최신으로 가장하지 않고 `/api/health`와 화면을 degraded 상태로 표시합니다.
- `collaboration_board.MEMBER_NAME`은 `MEMBER_ID=REG_ID` 전수 일치 결과에 따라 업무 담당자가 아닌 협업방 `개설자`로 표시합니다.
- 씽크와이즈 상태는 마지막 수정일로 자동 분류하고 수동 완료·진행·보류가 우선합니다.
- 수동 상태를 취소하면 자동 분류로 즉시 복귀합니다.
- 씽크와이즈는 1분, 메일은 5분 주기로 갱신합니다.
- 메일은 최근 안 읽은 메일의 제목·발신자·시각만 표시합니다.
- 외부 접속은 Tailscale Serve 비공개 HTTPS를 사용하고 앱 로그인 화면은 두지 않습니다.
- 승인 화면은 `경영대시보드_예시화면_v2.html`입니다.
- 경영 현황의 NAS 영역은 File Station 최근 파일이 아니라 UDP 514로 실제 수신된 범위의 참고용 활동 피드이며, 씽크와이즈 최근 변경과 같은 형식으로 나란히 표시하고 감사 원장으로 사용하지 않습니다.
- `rename` 등 직접 확인된 제한 토큰만 표시하고 생성·수정·이동·삭제 의미를 추론하지 않으며, 부족한 정보는 `NAS 작업 감지`·`대상 정보 없음`으로 표시합니다.
- NAS 로그 기능은 File Station 설정과 분리하고 허용 송신자 IPv4·datagram 크기·strict UTF-8·제어문자·대상 루트 검사를 통과한 정규화 필드만 기존 SQLite에 저장합니다.
- 원문 로그·송신자/NAS/클라이언트 IP·호스트명·프로토콜·절대 경로는 저장·로그·API·health에 남기지 않습니다.
- 기존 File Station 코드와 API는 검증 이력·롤백 판단을 위해 유지하지만 현재 UI에서는 호출하지 않으며 제거는 별도 승인 사항입니다.

## 자동 기록 규칙

1. **한 가지 목적의 변경을 한 건으로 기록합니다.** 같은 목적을 위해 여러 파일을 수정했다면 한 행에 묶습니다.
2. **변경과 검증을 함께 기록합니다.** 실행한 테스트·문법 검사·수동 확인을 적습니다.
3. **미검증은 그대로 표시합니다.** 실행하지 않은 테스트를 통과했다고 기록하지 않습니다.
4. **실패와 차단도 기록합니다.** 실패 원인과 다음 시도를 현재 작업 또는 차단 사항에 남깁니다.
5. **진행 요약을 함께 갱신합니다.** 마지막 갱신, 현재 단계, 다음 작업, Phase 상태가 변경 내용과 일치해야 합니다.
6. **비밀값은 기록하지 않습니다.** 자격증명은 `.env`에만 저장합니다.
7. 파일 변경이 없는 읽기 전용 조사는 변경 기록 대상에서 제외합니다.

## 변경 기록

| ID | 시각(KST) | 구분 | 변경 내용 | 영향 파일 | 검증·결과 |
|---|---|---|---|---|---|
| CHG-0048 | 2026-09-02 11:04 | 사용자 확인·세션 마감 | 사용자가 회사 공유 Drive와 Google 기본·씽크와이즈 전환 탭이 반영된 운영 화면을 확인하고 현재 상태로 대표이사 보고가 가능하다고 승인. 공용 할 일과 Google 일정·공유 Drive 요구 보강을 완료 처리하고 다음 세션 시작점을 대표이사 피드백 및 남은 Phase 4·5 운영 검증으로 현행화 | `경영대시보드_개발진행현황.md` | 사용자 실제 운영 화면 확인과 보고 가능 판단. 직전 검증 결과는 전체 69개 테스트, 실제 공유 scope·일정 5건·공유 파일 8건·stale=false, 운영 납품 검증, 비공개 HTTPS 200, Serve/Funnel tailnet only, 원격·서버 동기화 통과. 문서 전용 마감 변경이므로 Python 테스트는 재실행하지 않음 |
| CHG-0047 | 2026-09-02 10:07 | 회사 공유 Drive·화면 탭 구현·운영 반영 | 실제 OAuth 연결 뒤 최근 파일이 개인 Drive를 가리키는 문제를 회사 공유 Drive 전용 조회로 전환. 기존 메타데이터 읽기 전용 권한을 유지하면서 `corpora=drive`·공유 Drive ID·all-drives 옵션을 지정하고 운영 설정/API 검증에서 공유 scope를 강제해 개인 Drive 오조회를 막았다. 긴 화면은 `Google` 기본 탭과 `씽크와이즈` 탭으로 나누고 sticky 전환 버튼·키보드 이동·ARIA 선택/패널 상태·씽크와이즈 설정 버튼 표시를 동기화했다 | `.env.example`, `README.md`, `인수인계.md`, `app/config.py`, `app/domain.py`, `app/google_runtime.py`, `app/google_workspace.py`, `app/main.py`, `scripts/set_google_shared_drive.ps1`, `scripts/audit_delivery_readiness.ps1`, `scripts/verify_delivery.ps1`, `tests/test_api.py`, `tests/test_config.py`, `tests/test_google_workspace.py`, `경영대시보드_예시화면_v2.html`, 진행 현황, 운영 서버 `.env`·대시보드 작업 | 접근 가능한 공유 Drive가 1개임을 파일 메타데이터에서 읽기 전용 확인. 공유/개인 fallback 쿼리와 운영 설정 강제를 포함한 집중 30개·전체 69개 테스트, Python compileall, JavaScript 구문·HTML 고유 ID 71개, 변경 PowerShell 구문, `git diff --check` 통과. 저장소 가상환경은 원본 사용자 Python 경로 인코딩 문제로 실행 실패해 설치된 Python 3.11로 동일 검증 통과. 앱 내 브라우저 목록이 비어 자동 시각·클릭 검증은 미실행. 커밋 `9bbdadf` push·서버 fast-forward 뒤 첫 원격 stdin `.env` 입력은 성공 메시지와 달리 설정 행이 없어 운영 필수 검증이 의도대로 시작을 거부했고 예약 작업 상태 `Ready`·결과 1을 확인. 다른 설정을 보존하는 전용 무출력 스크립트를 커밋 `df86342`로 추가·배포해 설정 존재·Settings 검증·ACL·작업 재시작을 통과하고 서비스 복구. 최종 운영 API는 공유 scope·일정 5건·공유 파일 8건·stale=false·탭 구조를 반환했고 `verify_delivery.ps1 -RequireProduction`에서 프로젝트 96건·공유 색인·활성 메일 1계정·Google 통과. 서버 작업 트리 clean·HEAD `df86342`, 비공개 HTTPS 200, Serve와 Funnel 모두 `tailnet only` 확인 |
| CHG-0046 | 2026-09-02 08:58 | Google OAuth PKCE 콜백 수정 | 최초 실계정 동의가 콜백 502로 끝난 원인을 추적. 공식 라이브러리 1.4.1의 기본값이 인증 URL 생성 때 PKCE verifier를 자동 생성하지만 기존 코드는 콜백에서 새 Flow를 만들어 verifier를 잃고 있었다. verifier를 state별 10분짜리 프로세스 메모리에만 보존하고 콜백·거부 시 1회 소비하도록 수정해 비밀값 저장 원칙과 복수 동시 요청을 유지 | `app/google_oauth.py`, `tests/test_google_oauth.py`, `인수인계.md`, 진행 현황, 커밋 `021153a`, 운영 서버 | 비밀값 없는 서버 진단에서 Google 설정 활성화, Client ID·Secret 형식, Redirect URI 정확 일치, 토큰 서버 443 연결, 작업 실행을 확인하고 Refresh Token 미저장 확인. 공식 토큰 엔드포인트에 고의 무효 진단 코드 1건을 보내 예상된 `invalid_grant`를 받아 클라이언트 자격증명 승인을 확인. 설치 라이브러리 소스에서 `autogenerate_code_verifier=True`와 `fetch_token`의 verifier 전달 계약을 직접 확인. PKCE 연속성·재시작 시 안전한 재요청을 포함한 집중 27개, 전체 67개 테스트와 Python `compileall`, `git diff --check` 통과. 첫 다중행 원격 진단은 출력 없이 끝나 단일행 무출력 진단으로 재실행해 통과. `021153a` push·서버 fast-forward·대시보드 재시작 후 server HEAD 일치, core health `ok`, Google `configured=true`·`authorization_required`, 비공개 HTTPS 200 확인. 새 OAuth 동의·실데이터 검증은 대기 |
| CHG-0045 | 2026-09-01 17:13 | Google 코드·의존성 서버 반영 | 사용자의 4단계 실행 요청에 따라 전체 66개 테스트 통과본을 `feat: Google Calendar와 Drive 읽기 전용 연동`으로 커밋해 원격 게시 브랜치에 push하고, 깨끗한 운영 서버 작업 트리에 fast-forward pull. 대시보드·위키 공유 가상환경에 양쪽 요구사항을 함께 설치했으며 `.env`와 실행 중인 대시보드 작업은 변경하지 않음 | 커밋 `8a9ff90`, 원격·운영 서버 `C:\apps\yjs_backoffice`, 서버 공유 가상환경, `README.md`, `인수인계.md`, 진행 현황 | 스테이징 `diff --check` 통과, 원격 push와 서버 pull 성공, 로컬·서버 HEAD `8a9ff90` 일치. 서버에 `google-auth 2.57.0`, `google-auth-oauthlib 1.4.1`, `requests 2.34.2` 설치 확인 및 `pip check` 통과. 첫 의존성 설치 SSH는 샌드박스가 연결을 차단해 서버 변경 없이 실패 후 승인된 동일 명령으로 성공. 병렬 서버 HEAD 조회도 같은 차단 후 승인 실행으로 통과. 첫 import 한 줄 검사는 원격 셸 인용이 `-c` 코드를 잘라 `SyntaxError`로 실패했으나 인용 없는 `pip show`와 `pip check`로 설치 상태를 최종 확인. 후속 진행 문서도 별도 커밋으로 원격·서버에 fast-forward 반영하고 양쪽 작업 트리 clean을 확인했으며 기존 비공개 HTTPS health 200 유지. 첫 HTTPS 확인은 샌드박스 SSH 차단 후 승인된 동일 명령으로 통과. 앱 재시작·운영 `.env`·OAuth 동의·실데이터 API·브라우저 검증은 미실행 |
| CHG-0044 | 2026-09-01 16:29 | Google 일정·Drive 수직 슬라이스 | 대표이사 본인 계정의 이번 주 Calendar 일정과 Drive 최근 수정 파일을 한 번의 OAuth로 읽는 기능을 구현. Calendar 읽기 전용·Drive 메타데이터 읽기 전용 범위, 개인 사용자 corpus·폴더/휴지통 제외·최근 수정 8건, 5분 독립 캐시와 마지막 성공 유지, 해시·10분 만료·1회 소비 OAuth state, 기존 ACL을 보존하는 `.env` 갱신 토큰 저장, 연결 시작/콜백/API/health를 추가. `목표.html`에 맞춰 상단을 720px 일정→Drive→할 일 순서와 480px 통계 2열로 구성하고 씽크와이즈·메일은 아래에 유지. 운영 검증·감사 스크립트와 연결 절차를 갱신 | `.env.example`, `requirements.txt`, `README.md`, `인수인계.md`, `app/config.py`, `app/domain.py`, `app/google_oauth.py`, `app/google_runtime.py`, `app/google_workspace.py`, `app/main.py`, `app/service.py`, `app/state_store.py`, `scripts/audit_delivery_readiness.ps1`, `scripts/verify_delivery.ps1`, `tests/test_api.py`, `tests/test_config.py`, `tests/test_google_oauth.py`, `tests/test_google_runtime.py`, `tests/test_google_workspace.py`, `tests/test_state_store.py`, `경영대시보드_예시화면_v2.html`, 진행 현황 | 집중 35개와 최종 전체 66개 테스트 통과, Python `compileall`, `pip check`, PowerShell 전체 구문, JavaScript 구문, HTML ID 65개·중복 0개·JS 고아 참조 0개, `git diff --check` 통과. 로컬 demo 실제 HTTP에서 `/` 200·Google 일정 2건·파일 2건·health `ok` 확인. JavaScript·ID 통합 검사 첫 PowerShell 명령은 정규식 셸 해석으로 실행 전 실패하고 cmd 재시도도 인용으로 실행 전 실패하며 빈 검사 부산물 1개를 만들어 확인 후 삭제했으나, PowerShell 추출 구문 검사와 네이티브 ID 대조로 각각 최종 통과. Ruff는 설치돼 있지 않아 미실행. Google Cloud 자격증명과 실제 계정 동의가 없어 실 API·운영 배포 미실행, 연결 가능한 앱 내 브라우저가 없어 데스크톱·모바일 시각·클릭 미실행. 사용자가 잔여 작업의 중간 게이트를 생략하라고 명시해 설계·구현·검증을 연속 수행 |
| CHG-0043 | 2026-09-01 16:10 | 공용 할 일 운영 배포 | 공용 할 일 커밋 `e90645a`를 원격 게시 브랜치와 운영 서버에 fast-forward 반영하고 대시보드 작업만 재시작. 서버 SQLite 영속 목록을 비공개 Tailscale HTTPS API로 추가·조회·삭제해 운영 수직 슬라이스 확인 | 원격·운영 서버 `C:\apps\yjs_backoffice`, 대시보드 작업, 진행 현황 | 서버 작업 트리 clean·브랜치 일치 후 pull·재시작 통과. 운영 검증에서 프로젝트 96건·공유 SQLite 색인·활성 메일 1계정 통과, `/api/todos` GET 200. 첫 두 curl POST는 Windows JSON 인용 문제로 요청이 실패해 행이 생성되지 않았고, `Invoke-RestMethod`로 재실행해 생성 1건·목록 1건·삭제 204·최종 0건 확인. 실제 브라우저 시각·클릭은 미실행 |
| CHG-0042 | 2026-09-01 15:52 | 대표이사 공용 할 일 영속화 | 티어 A 게이트 1에서 대표이사 공용 단일 목록과 완료 즉시 제거를 선택. 기존 SQLite에 `todo_item`을 추가하고 서비스·추가/조회/삭제 API·1~120자 입력 검증을 연결했으며, 모든 허용 Tailscale 계정이 같은 목록을 보되 기존 동일 출처 보호를 유지. `목표.html`의 720px 카드·입력·체크·빈 상태 디자인을 경영 화면 상단에 적용하고 완료/삭제 중복 요청 방지와 60초 공용 목록 재동기화를 구현. 완료 이력과 진행률은 선택에 따라 저장하지 않음 | `app/domain.py`, `app/main.py`, `app/service.py`, `app/state_store.py`, `tests/test_api.py`, `tests/test_state_store.py`, `경영대시보드_예시화면_v2.html`, 진행 현황 | 변경 전 전체 50개, 변경 후 집중 16개와 전체 54개 테스트 통과. Python `compileall`, JavaScript 구문, HTML ID 52개·중복 0개·JS 고아 참조 0개, `git diff --check` 통과. 실제 localhost HTTP에서 화면·health 200/ok, 추가 201·목록 1건·삭제 204·최종 본문 `[]` 확인. 첫 빈 목록 개수 검사는 PowerShell이 JSON `[]`를 `$null` 한 요소로 감싸 1건으로 오판해 응답 본문 직접 비교로 재검증 통과. 저장소 가상환경과 샌드박스 Python 경로 실패는 시스템 Python 실제 사용자 실행으로 우회. HTML ID 대조 명령 2회는 PowerShell 인용 파싱으로 실행 전 실패 후 네이티브 정규식으로 통과. 앱 내 브라우저 목록이 비어 데스크톱·모바일 시각·클릭과 운영 서버 배포는 미실행. 게이트 3에서 즉시 삭제·허용 계정 공동 수정·진행률 제외를 모두 수용. 게이트 4에서 화면만 지우면 SQLite 항목이 재출력되는 이유를 자력 설명했고, 동일 출처 검사는 동시 수정 방지가 아니라 요청을 시작한 주소를 확인해 외부 악성 페이지를 거부하는 장치임을 힌트 1회 후 재설명해 통과. 게이트 5에서 사용자가 메시지 초안 작성과 커밋을 위임해 `feat: 대표이사 할 일을 공용 목록으로 영속화`로 한 커밋 확정 |
| CHG-0041 | 2026-09-01 15:28 | 인수인계 문서 통합 | 흩어져 있던 인수인계·배포 문서 3종(`서버PC_Codex_인수인계.md` 505줄, `배포_체크리스트.md` 193줄, `서버_유지보수_배포.md` 141줄)을 `인수인계.md` 하나로 통합. 앞의 둘은 최초 구축용이라 이미 소진됐고, 세 번째의 운영 런북(반영 순서·변경 종류별 재시작·배포 후 검증·실패 시 복구)과 체크리스트에만 있던 ACL·백업 절차를 흡수했다. 여기에 다음 세션이 바로 이어갈 수 있도록 요구 정본(`목표.html`)·확정 결정 5건·다음 작업과 미결정·검증 요령·함정을 더했다. `README.md`와 `AGENTS.md`의 링크를 새 문서로 교체 | `인수인계.md`(신규), `README.md`, `AGENTS.md`, 삭제: `서버PC_Codex_인수인계.md`·`배포_체크리스트.md`·`서버_유지보수_배포.md`, 진행 현황 | 삭제 전 세 문서의 목차와 상호 참조를 조사해, 살아있는 참조가 `서버_유지보수_배포.md` → `배포_체크리스트.md`의 ACL·백업 절차 1건임을 확인하고 새 문서에 흡수. 삭제 후 저장소 전체에서 세 파일명 참조 0건 확인(과거 변경 기록의 서술은 이력이므로 유지). 전체 50개 테스트 통과. 비밀값·SSH 비밀번호·배포 키·메일 주소는 새 문서에 기록하지 않음 |
| CHG-0040 | 2026-09-01 15:34 | 요구 정본 확정 · NAS 구현 철회 | 대표이사가 직접 작성한 `목표.html`을 요구 정본으로 확정하고 NAS 관련 구현을 전부 철회. 작업 트리를 `1805108`로 되돌려 UDP 활동 수신기(`nas_activity*`), File Station 파일럿(`nas.py`·`nas_service.py`·`nas_runtime.py`), `NAS_LOG_*`/File Station 설정, 화면 카드와 관련 테스트 27개를 제거했다. NAS 무관한 제목 개명(`오늘 건드린 내용` → `씽크와이즈 최근 변경`)만 되살렸다. 부수로 기존 결함 1건 수정 — `core.autocrlf=true`에 `.gitattributes`가 없어 깨끗한 체크아웃에서는 본문이 CRLF가 되는데 화면 계약 테스트가 LF 기준 여러 줄 문자열을 단언하고 있었다(이전 작업본이 우연히 LF였던 탓에 가려져 있었음). 해당 단언을 정규화 후 비교하도록 고쳤다 | `.env.example`, `README.md`, `app/config.py`, `app/domain.py`, `app/main.py`, `app/state_store.py`, `tests/test_api.py`, `tests/test_config.py`, `tests/test_state_store.py`, `경영대시보드_기획서.md`, `경영대시보드_예시화면_v2.html`, 삭제: `app/nas*.py` 5개·`tests/test_nas*.py` 5개, 진행 현황 | 전체 50개 테스트 통과(NAS 27개 제거 반영). 되돌리기 전 미커밋 작업 전체를 스크래치패드에 백업하고 diff를 검사해 NAS 무관 변경이 제목 개명 1건뿐임을 확인. HTML은 바이트 단위로 편집해 CRLF 1,356줄을 그대로 유지, 불필요한 공백 diff 0건. 삭제 대상은 사용자 지시로 확인받고 실행했으며 실제 `.env`·방화벽·DSM·서버 배포는 변경하지 않았고 NAS 쓰기·커밋·push 0건 |
| CHG-0039 | 2026-09-01 10:48 | 경영 현황 활동 피드 통합·수신 설정 안내 | 별도 `주간 현황` 탭과 전환 코드를 제거하고 `먼저 확인할 프로젝트` 아래에 씽크와이즈·NAS 최근 활동을 같은 `작업 배지 / 제목·메타 / 시각` 행 형식, 각 최대 6건으로 나란히 배치. NAS의 rename·unknown·대상 없음·상태별 안내와 참고용 계약은 유지하고 기존 File Station UI 호출은 계속 비활성화. README와 환경 예시에 DSM Log Center·별도 `NAS_LOG_*`·NAS 단일 IPv4 방화벽·재시작·확인 순서를 추가하고 기획 문서를 단일 경영 화면 기준으로 현행화 | `.env.example`, `README.md`, `tests/test_api.py`, `경영대시보드_예시화면_v2.html`, `경영대시보드_기획서.md`, `대표이사_주간현황_NAS연동_기획서.md`, 진행 현황 | 화면 계약 집중 테스트 12개와 전체 77개 통과, Python `compileall`, JavaScript 구문, HTML 고유 ID 52개·중복 0개, 탭/레거시 UI 참조 0건, `git diff --check` 통과. 실행 중인 127.0.0.1:8080은 종료·재시작하지 않고 HTTP 200·두 피드·NAS 6건 호출·주간 탭 부재를 확인했으며 상태 API에서 현재 `enabled=false`, `status=disabled`, `listening=false`, 저장·수신 0건을 민감값 없이 확인. 첫 Python 실행은 샌드박스에서 사용자 설치 경로를 보지 못했고 `py`도 설치본을 찾지 못해 권한 있는 동일 Python으로 재실행해 통과. 첫 HTML ID 정규식은 0건으로 무효여서 수정 후 재검증. 연결 가능한 앱 내 브라우저가 없어 시각 검증은 미실행. 실제 `.env`·방화벽·DSM·서비스 상태는 변경하지 않았고 NAS 쓰기·관리자 계정·커밋·push·배포 0건 |
| CHG-0001 | 2026-08-14 | 기획 | 제약 조사, 상태 판정, 메일 완전안, 구현 단계 정리 | `경영대시보드_기획서.md` | 문서 검토 완료 |
| CHG-0002 | 2026-08-14 | 설계 | 정보 우선순위·검색·상태 필터·반응형을 반영한 목업 작성 | `경영대시보드_예시화면_v2.html` | HTML 구조·JS 문법 검사 통과 |
| CHG-0003 | 2026-08-14 | 설계 | 최근 활동 링, 프로젝트 상태 도넛, 메일 계정별 막대 차트 추가 | 기획서, 승인 목업 | 태그 균형·ID·ARIA 참조·JS 문법 검사 통과 |
| CHG-0004 | 2026-08-14 16:01 | 문서·운영 | 자동 진행 기록 정본과 Codex·Claude 저장소 규칙 추가, README 현행화 | `경영대시보드_개발진행현황.md`, `AGENTS.md`, `CLAUDE.md`, `README.md` | 문서 링크 4건·필수 섹션·코드 펜스 검사 통과 |
| CHG-0005 | 2026-08-14 16:30 | 구현 | FastAPI 골격, 설정, SELECT 전용 MariaDB 계층, 상태 계산, 독립 메모리 캐시, 데모 데이터와 보안 헤더 구현 | `app/`, `run.py`, `.env.example`, `requirements*.txt`, `pyproject.toml`, `scripts/validate_phase0.py` | 최초 테스트의 Windows 시간대 데이터 누락은 `tzdata`로 해결. 진단 스크립트 직접 실행 시 모듈 경로 오류를 수정한 뒤 전체 Python 문법 검사와 무자격증명 안전 중단 확인. 최종 전체 테스트 21개 통과, 의존성 향후 호환성 경고 1건 |
| CHG-0006 | 2026-08-14 16:29 | 구현·화면 | 승인 목업의 KPI·차트·메일·정체 프로젝트·활동·사용자·표를 API 데이터로 갱신하고 60초 자동 새로고침 연결 | `경영대시보드_예시화면_v2.html`, `tests/test_api.py` | JavaScript 문법, 중복 ID, 주요 태그 균형 검사 및 로컬 HTTP 상태 응답 200 통과. 화면 연결 테스트 조건 1건 불일치 수정 후 전체 21개 통과. 연결 가능한 브라우저가 없어 시각 캡처는 미실행 |
| CHG-0007 | 2026-08-14 16:31 | 문서 | 데모 실행·테스트·실데이터 사전 진단 명령과 현재 구현 범위 현행화 | `README.md`, `경영대시보드_개발진행현황.md` | 첫 문서 점검 명령의 PowerShell 조건식 오류를 바로잡은 뒤 구현 전 표현 제거, 코드 펜스 균형, 임시 서버·로그 정리 확인 완료 |
| CHG-0008 | 2026-08-14 16:39 | 세션 인계 | 다음 세션 시작 조건과 실데이터 미검증 범위를 명시하고 커밋 전 파일 끝 공백 정리 | 진행 현황 및 신규 설정·Python·테스트 파일 | `git diff --check` 지적 사항 정리 후 전체 테스트 21개 통과, 의존성 향후 호환성 경고 1건 |
| CHG-0009 | 2026-08-18 14:48 | 실데이터 검증·설계 | SELECT 전용 Phase 0 진단과 성능 측정으로 `UPD_DATE` 불일치를 확인하고, 위키가 생성한 단일 SQLite 작업 이력 색인을 대시보드가 읽기 전용 재사용하도록 전환 | `app/worklog_index.py`, `app/repository.py`, `app/config.py`, `.env.example`, `tests/test_worklog_index.py` | `work_log.indx` PK 확인, 539,231행 범위·원본 풀 집계 약 32초 확인. 87개 중 `UPD_DATE` 5분 일치 10개라 최종 활동 기준에서 제외. 공유 색인 프로젝트 집계 약 1.3초, 최근 활동 약 50ms. 색인 갱신 후 실데이터 API 200·source healthy·stale=false 통과 |
| CHG-0010 | 2026-08-18 15:01 | 상태·설정 | 대시보드 전용 SQLite에 수동 상태·설정 저장, 표시 취소, API, 승인 화면 설정 대화상자와 상태 선택, 일일 백업·30일 보관 구현 | `app/state_store.py`, `app/domain.py`, `app/service.py`, `app/main.py`, `경영대시보드_예시화면_v2.html`, `tests/test_api.py`, `tests/test_state_store.py` | 수동 상태 적용·취소, 임계값 저장, 프로세스 재생성 후 유지, SQLite 백업 복원 조회 시험 통과. 연결 가능한 인앱 브라우저가 없어 시각·클릭 검증은 미실행 |
| CHG-0011 | 2026-08-18 15:06 | 메일 | 다우오피스·Gmail·네이버 IMAP 헤더 수집, 안 읽음 검색, 계정별 장애 격리, MIME·KST 변환과 독립 5분 워커 구현 | `app/mail.py`, `app/config.py`, `app/service.py`, `app/runtime.py`, `app/main.py`, `.env.example`, `tests/test_mail.py` | 가짜 IMAP으로 `readonly=True`, `UNSEEN`, `BODY.PEEK`, MIME 제목, 계정 1개 실패 시 나머지 유지 확인. 실데이터 모드 서비스 연결 결함을 후속 시험에서 발견·수정. 실제 메일 계정 검증은 자격증명 대기 |
| CHG-0012 | 2026-08-18 15:27 | 배포·보안 | localhost 강제, 운영 모드의 실데이터·Tailscale 헤더 검증 강제, 공유 색인·대시보드 SYSTEM 자동시작, Serve 구성·납품 검증 스크립트와 배포 체크리스트 작성 | `app/config.py`, `.gitignore`, `scripts/*.ps1`, `배포_체크리스트.md`, `README.md`, `tests/test_config.py`, `tests/test_api.py` | 전체 테스트 34개 통과, Python compileall·JavaScript·PowerShell 5개 파일 구문·`git diff --check` 통과. Starlette 향후 호환성 경고 1건. 예약 작업 객체 생성 검증은 샌드박스 WMI 접근 거부로 실패해 관리자 환경 시험으로 이관. 이 PC에 Tailscale이 없어 실제 Serve·재부팅·외부망 시험은 미실행 |
| CHG-0013 | 2026-08-18 15:22 | 통합 검증 | 위키 공유 색인을 증분 최신화하고 비밀값을 출력하지 않는 실데이터 회귀 시험 수행 | 공유 색인 운영 데이터, 진행 현황 | 증분 9행 반영 후 `/api/dashboard`·`/api/health` 200, runtime `ok`, shared index healthy, stale=false, 프로젝트 87건·최근 변경 30건 확인. 메일은 비활성 상태로 실계정 검증 미실행 |
| CHG-0014 | 2026-08-18 15:40 | 납품 감사·보완 | 씽크와이즈 최근 변경을 1분 주기로 조정하고 색인 신선도 3분 제한, 메일 실패 시 마지막 성공 데이터 보존·실패 시각 표시, 설정된 접속 판정 시간의 화면 반영, 백업 정확히 30일 보관, 운영 공유 색인·메일 HTTPS URL·Origin 보안 강제, 단일 가상환경 자동시작으로 보완 | `app/config.py`, `app/main.py`, `app/runtime.py`, `app/security.py`, `app/state_store.py`, `.env.example`, `requirements.txt`, `scripts/install_startup_tasks.ps1`, 승인 화면, 배포 체크리스트, `tests/` | 신규 회귀를 포함한 전체 40개 테스트 통과, Python·JavaScript·PowerShell 구문, HTML 고유 ID 43개·`innerHTML` 미사용, `git diff --check` 통과. 실데이터에서 work_log 주기 60초·source healthy·stale=false 확인 |
| CHG-0015 | 2026-08-18 15:51 | 데이터 의미 확정 | Phase 0 진단에 `MEMBER_ID`·`REG_ID`·참여자 관계 집계를 추가하고 API·화면·기획서의 잘못된 `담당/owner` 표현을 `개설자/creator`로 정정 | `scripts/validate_phase0.py`, `app/domain.py`, `app/repository.py`, `app/service.py`, 승인 화면, 기획서, `tests/test_api.py` | 첫 확장 진단은 SQL `LIKE`의 `%` 파라미터 이스케이프 누락으로 TypeError 실패 후 수정. 재실행에서 87/87 `MEMBER_ID=REG_ID`, 사용자 신원 85/87, 참여자 2/87 확인. 전체 40개 테스트 통과, 실데이터 프로젝트 87건 모두 creator 존재·legacy owner 0건·runtime `ok` 확인 |
| CHG-0016 | 2026-08-18 16:11 | 상태 정확도·화면 | 프로젝트 최종 활동에서 열람(`Link_Open`)·인쇄(`Col_Print`)를 제외해 실제 변경 6종만 상태를 움직이도록 수정하고, 최근 활동 사용자에 마지막 프로젝트를 표시하며 전체 갱신 실패 데이터 나이를 화면에 추가 | `app/worklog_index.py`, 승인 화면, `tests/test_worklog_index.py`, `tests/test_api.py` | 전체 40개 테스트·Python·JavaScript 구문 통과. 분리 실행 두 번은 승인 지연으로 색인 나이 448초가 되어 의도대로 degraded 판정됨. 동기화와 API를 한 프로세스에서 연속 실행한 최종 시험은 source age 1초, runtime `ok`, stale=false, 프로젝트 87건·최근 변경 30건 통과 |
| CHG-0017 | 2026-08-18 16:13 | 운영 보안 | 대시보드·위키 `.env`와 두 SQLite 데이터 폴더를 SYSTEM·관리자·현재 배포 사용자로 제한하는 범위 검증 ACL 스크립트를 추가하고, Tailscale Grants를 대표이사 계정뿐 아니라 지정 PC·휴대폰 Tailscale IP 원본 별칭으로도 제한하도록 체크리스트 강화 | `scripts/secure_runtime_acl.ps1`, `배포_체크리스트.md` | PowerShell 전체 구문 검사 통과. ACL 실제 적용과 Grants 정책 저장은 관리자 권한·Tailscale이 없어 미실행이며 현장 인수 시험으로 유지 |
| CHG-0018 | 2026-08-18 16:14 | 감사·오류 처리 | 설정별 `updated_at` 감사 시각과 기존 SQLite 자동 마이그레이션을 추가하고, 해석 불가능한 메일 날짜를 목록 끝에 유지하면서 제목·주소 없이 건수만 경고 기록 | `app/state_store.py`, `app/mail.py`, 기획서, `tests/test_state_store.py`, `tests/test_mail.py` | 구버전 setting 테이블 마이그레이션·백업 복원 감사 시각·메일 날짜 2건 오류 로그와 개인정보 비노출 시험을 포함한 전체 42개 테스트 통과 |
| CHG-0019 | 2026-08-18 16:18 | 시각 검증 재시도 | localhost:8090 데모 서버로 승인 화면 시각·클릭 검증을 다시 시도 | 진행 현황 | 데모 서버 HTTP 준비와 종료·포트 정리 확인. Browser 런타임의 연결 가능 브라우저 목록이 비어 있어 스킬 지침상 시각·클릭 검증은 미실행으로 유지하고 다른 브라우저 자동화로 우회하지 않음 |
| CHG-0020 | 2026-08-18 16:25 | 납품 준비 자동 감사·Windows 호환성 | 비밀값·SQLite ACL을 상속 제거 후 SYSTEM·관리자·배포 사용자에게만 명시 부여하도록 강화하고, 운영 설정·공유 색인·API 보안·실메일·백업·예약 작업·ACL·Tailscale Serve·Funnel을 비밀값 출력 없이 읽기 전용 검사하는 감사 스크립트 추가. Windows PowerShell 5.1의 BOM 없는 UTF-8 메시지 깨짐을 막기 위해 모든 배포 스크립트 콘솔 문구를 ASCII로 통일하고 다우오피스 URL 예시를 안전한 빈 템플릿으로 정리 | `scripts/secure_runtime_acl.ps1`, `scripts/audit_delivery_readiness.ps1`, 기타 배포 `scripts/*.ps1`, `.env.example`, `배포_체크리스트.md`, 진행 현황 | 전체 42개 테스트·Python compileall·JavaScript·PowerShell 구문·HTML 고유 ID 43개와 안전 DOM·Markdown 펜스·`git diff --check` 통과. 실제 Windows 감사에서 Python 의존성·오늘자 SQLite 읽기 전용 백업은 통과했으나 운영 `.env`·실행 서비스·실메일·예약 작업·ACL·Tailscale이 이 PC에 없어 12개 항목은 실패 상태로 정확히 유지. 시각·클릭 검증도 연결 브라우저 부재로 미실행 |
| CHG-0021 | 2026-08-18 16:54 | 요구사항 완료 감사·신뢰성 보완 | 최근 변경 워커 최신성까지 전체 상태에 반영하고 동시 핵심 갱신이 새 최근 변경·메일을 덮지 않도록 병합 순서를 수정. 공유 이력의 실제 최종 수정순 재정렬·색인 스키마 계약 검사, 오래 멈춤 기본 접기·검색 시 포함·실데이터 최초 로딩/빈 값/실패 화면·유효 받은편지함 링크·메일 0건 차트, 매일 1회 및 상태 변경 시 SQLite 백업을 구현. import만으로 DB가 생기지 않는 Uvicorn 팩토리, Serve HTTPS 종단 호환 Origin 검증·localhost 프록시 신뢰·HSTS, 다우 전체 메일 주소, 비민감 Tailscale 장치 이름·Funnel 사전 거부, 기존 DB·백업 포함 재귀 ACL과 강화된 자동 감사를 반영하고 TestClient를 공식 권장 `httpx2`로 전환 | `app/config.py`, `app/main.py`, `app/repository.py`, `app/runtime.py`, `app/security.py`, `app/service.py`, `app/state_store.py`, `app/worklog_index.py`, `run.py`, 승인 화면, `scripts/*.ps1`, `requirements-dev.txt`, 배포 체크리스트, `tests/`, 진행 현황 | 백업 검증 1건이 Windows의 SQLite 기존 파일 수정 시각 보존 때문에 실패해 파일 시각 대신 백업 내부 행 복원 검증으로 수정. 최종 전체 49개 테스트 경고 없이 통과, `pip check`, Python compileall, JavaScript, PowerShell 5.1 ASCII·구문, HTML 고유 ID 45개·안전 DOM, Markdown 펜스, `git diff --check` 통과. 실제 공유 색인 증분 1행 반영 후 539,250행·source age 0초·스키마 계약·최근 변경 30건 통과. 실데이터 API 87건·개설자 87건·최근 변경 30건·상태 합계·실제 최종 수정순·runtime `ok` 확인. SELECT별 직접 계측은 프로젝트 1.082초, 나머지 각 0.002초 이하. 강화된 Windows 감사는 의존성·오늘자 백업 2건 통과, 운영 `.env`·서비스·메일·작업·ACL·Tailscale 부재 관련 13건 실패를 정확히 유지. 실제 브라우저 검증은 미실행 |
| CHG-0022 | 2026-08-18 17:15 | 서버 배포 이식성 | 납품 위치를 씽크와이즈 DB에 접근 가능한 상시 가동 Windows 서버 PC로 확정하고, 사용자명·드라이브에 종속된 절대 경로를 설치 루트 변수와 저장소 간 상대 경로로 교체. 자동시작 등록과 최종 감사에서 위키·대시보드가 실제로 동일한 공유 색인 파일을 사용하는지 비교해 잘못된 이중 조회·낡은 별도 색인 구성을 거부하도록 보강 | `.env.example`, `README.md`, `배포_체크리스트.md`, `scripts/install_startup_tasks.ps1`, `scripts/audit_delivery_readiness.ps1`, 진행 현황 | 전체 49개 테스트 통과, PowerShell 전체 ASCII·구문, 개발 PC 절대 경로 제거, Markdown 펜스, `git diff --check` 통과. 첫 `git -C` 검사는 샌드박스 저장소 소유권 판정으로 실패해 전역 설정을 바꾸지 않고 명령 단위 `safe.directory`로 재실행 후 통과. 읽기 전용 납품 감사는 새 공유 색인 일치 항목을 포함해 이 개발 PC에 없는 운영 `.env`·서비스·실메일·예약 작업·ACL·Tailscale 관련 13개 실패를 정확히 유지. 실제 서버 설치는 접속 환경 대기 |
| CHG-0023 | 2026-08-18 17:25 | 서버 Codex 인수인계·환경 호환성 | 새 서버 세션이 사용자 재설명 없이 바로 실행하도록 목표·범위·확정 아키텍처·현재 검증값·미커밋 코드 전달 위험·보안 원칙·필요 입력·명령·중단/복구·완료 기준을 단일 인수인계 문서로 작성. 공유 가상환경에 두 저장소 요구사항을 함께 설치하도록 절차를 보완하고 최종 감사에서 대시보드와 위키 동기화 모듈을 모두 import하는지 검사 | `서버PC_Codex_인수인계.md`, `README.md`, `배포_체크리스트.md`, `scripts/audit_delivery_readiness.ps1`, 진행 현황 | 전체 49개 테스트 통과, 위키 `app.sync` import 성공, PowerShell 전체 ASCII·구문 통과. 첫 직접 import는 샌드박스 계정이 기존 가상환경 Python 원본 경로를 찾지 못해 실패했으며 실제 사용자 환경으로 같은 명령을 재실행해 통과. 첫 스테이징 `diff --check`는 인수인계 머리말의 Markdown 줄바꿈 공백 2건을 지적해 제거 후 재검증. 읽기 전용 납품 감사에서 새 두 앱 import 항목과 오늘자 백업은 PASS, 이 개발 PC에 없는 운영 `.env`·서비스·실메일·예약 작업·ACL·Tailscale 관련 13개 항목은 의도대로 FAIL 유지. 실제 서버 배포는 최신 작업 트리 전달·서버 접속 대기 |
| CHG-0024 | 2026-08-18 17:35 | 원격 게시·인계 현행화 | 전체 납품 구현을 별도 게시 브랜치에 커밋·push하고 draft PR을 생성한 뒤, 서버 인수인계의 미커밋 경고를 실제 브랜치 clone 절차와 구현 커밋 기준으로 교체 | 전체 변경 36개 파일, `서버PC_Codex_인수인계.md`, 진행 현황 | 고신뢰 비밀 패턴 없음, `.env`·가상환경·SQLite·런타임 데이터 미포함, 스테이징 `diff --check`, 전체 49개 테스트, `pip check`, PowerShell 구문 통과. 브랜치 `agent/management-dashboard-delivery`, 구현 커밋 `87fa8e9`, draft PR #2 생성 및 원격 커밋 일치 확인. 서버 실계정·실기기 검증 전이므로 PR은 draft 유지 |
| CHG-0025 | 2026-08-18 17:44 | 세션 마감 | 오늘의 구현·자동 검증·원격 게시·서버 인수인계 완료 상태를 확정하고, 사용자 일정에 따라 실제 서버 설치 시작을 2026-08-19로 이관 | 진행 현황 | 문서 Markdown 구조와 `git diff --check` 검증. 소스 변경이 없는 마감 기록이므로 Python·브라우저·실서버 검증은 재실행하지 않았으며, 직전 결과는 전체 49개 테스트 통과·서버 실계정/실기기 시험 대기 상태 유지 |
| CHG-0026 | 2026-08-19 10:48 | Windows 서버 배포 | 서버에 읽기 전용 배포 키·게시 브랜치·전용 Python 환경·운영 설정·ACL·Tailscale을 구성하고, 기존 위키 웹은 유지한 채 공유 색인과 대시보드를 SYSTEM 자동시작 작업으로 전환. 비밀값과 실제 로그인 식별자는 기록하지 않음 | 서버 `C:\apps\yjs_backoffice`, 서버 예약 작업·Tailscale 설정, 진행 현황 | 서버 전체 49개 테스트·`pip check`·대시보드/위키 import 통과. SELECT 전용 Phase 0에서 프로젝트 87건과 스키마 의미 재확인, 공유 색인 539,335행 증분 동기화 성공. 운영 API `ok`·프로젝트 87건·공유 색인 healthy·stale=false, 무인증 403, 오늘자 백업 조회, localhost listener와 두 신규 SYSTEM 작업 실행 확인. 다우오피스는 성공했으나 Gmail·Naver IMAP은 실패. Serve는 tailnet 기능 비활성화로 관리자 승인 대기이고 Funnel 설정은 없음. 전체 감사는 60초 및 재시도 3분을 초과해 중단했으며 재부팅·실기기·브라우저 시험은 미실행. 문서 갱신 후 `git diff --check` 통과 |
| CHG-0027 | 2026-08-19 11:21 | 보안 탐지 대응·임시 메일 범위 | Gmail·Naver 자격증명 확인 전 두 계정을 서버 운영 설정에서 비활성화하고 다우오피스만 시연 대상으로 유지. 원격 `EncodedCommand`가 보안 제품의 PowerShell 파일리스 행동 탐지와 같은 시각에 차단된 사실을 확인해 해당 방식을 폐기하고, 운영 검증 스크립트가 `.env`의 허용 로그인 값과 활성 메일 목록을 출력 없이 직접 읽어 신선한 메일 스냅샷까지 검사하도록 변경 | 서버 `.env`·대시보드 작업, `scripts/verify_delivery.ps1`, `배포_체크리스트.md`, 진행 현황 | Gmail·Naver 비활성화·ACL 재적용·대시보드 재기동 후, 평문 `-File` 검증에서 프로젝트 87건·공유 색인·활성 메일 1계정의 신선한 스냅샷 통과. PowerShell 구문과 `git diff --check` 통과. Tailscale 실행 파일 직접 호출에서도 tailnet Serve 비활성화가 확인되어 관리자 콘솔 HTTPS/Serve 활성화 대기. 보안 예외는 추가하지 않았고 재부팅·실기기 검증은 미실행 |
| CHG-0028 | 2026-08-19 11:43 | 비공개 HTTPS 배포·감사 호환성 | Owner 권한으로 tailnet HTTPS/Serve를 활성화하고 서버 localhost 대시보드를 tailnet 전용 HTTPS로 게시. 최신 Tailscale CLI가 비공개 Serve 경로도 `funnel status`에 표시하는 동작에 맞춰 공개 여부를 `Available on the internet` 표식으로 판정하도록 감사를 수정 | 서버 Tailscale Serve 설정, `scripts/audit_delivery_readiness.ps1`, 진행 현황 | 서버 직접 실행 파일 호출로 Serve 상태가 tailnet only이고 공개 Funnel 표식이 없음을 확인. HTTPS 상태 API 200과 HSTS·CSP·nosniff·DENY·no-referrer 헤더 통과, Tailscale 미접속 개발 PC에서는 전용 호스트 DNS 해석 실패 확인. PowerShell 인코딩 명령과 보안 예외는 사용하지 않음. PowerShell 구문과 `git diff --check` 통과. 실제 허용 사용자 브라우저·Grants·재부팅 검증은 아직 미실행 |
| CHG-0029 | 2026-08-19 13:23 | 납품 버전 1차 사용자 인수 | 허용 사용자 Windows PC에 Tailscale을 설치하고 같은 tailnet에 연결한 뒤 비공개 HTTPS 대시보드 화면 표시를 확인해 기획 납품 버전의 서버 배포와 1차 사용자 인수 완료 상태를 기록. 대표이사 계정·기기 Grants, Gmail·Naver, 비허용 사용자 거부와 새벽 재부팅은 후속 운영 검증으로 유지 | `경영대시보드_개발진행현황.md` | 사용자가 실제 PC 브라우저에서 화면 정상 표시와 결과 양호를 확인. `git diff --check` 통과. 문서 전용 변경으로 소스 테스트는 미실행 |
| CHG-0030 | 2026-08-19 13:42 | 화면 여백·메일 가독성 개선 | 먼저 확인할 프로젝트 아래에 남던 빈 공간을 없애기 위해 최근 안 읽은 메일 카드를 두 열 전체 폭의 다음 행으로 옮기고, 데스크톱에서는 메일을 2열로 배치해 화면 표시 상한을 5건에서 10건으로 확대. 모바일은 기존처럼 1열 유지 | `경영대시보드_예시화면_v2.html`, `tests/test_api.py`, 진행 현황 | 전체 49개 테스트·JavaScript 구문·`git diff --check` 통과. 첫 JavaScript 검사 명령은 정규식 이스케이프 오류로 실패 후 단순 추출 방식으로 재검증해 통과. 서버 `pull --ff-only` 반영 후 10건 제한과 전체 폭 패널 코드를 확인하고 비공개 HTTPS 상태 API 200 통과. 로컬 가상환경의 원본 Python 경로 인코딩 문제와 연결 가능한 브라우저 부재로 자동 시각 검증은 미실행이며 사용자 화면 확인 대기 |
| CHG-0031 | 2026-08-19 13:48 | 서버 유지보수·즉시 배포 런북 | 실제 운영 서버의 SSH 접점·경로·브랜치·예약 작업을 기준으로 로컬 수정부터 검증·push·서버 `pull --ff-only`·변경 종류별 재시작·운영 확인·안전한 revert까지 한 문서로 정리. 미래 Codex가 자동으로 읽도록 저장소 지침과 README에 연결하고 현재 운영 상태 문구도 현행화 | `서버_유지보수_배포.md`, `AGENTS.md`, `README.md`, 진행 현황 | 비밀번호·토큰·메일 주소·키 원문을 기록하지 않음. 실제 서버 작업 트리 clean·운영 브랜치·`pull --ff-only`·HTTPS 200으로 런북 핵심 경로 검증. Markdown 링크 대상·코드 펜스 14개·`git diff --check` 통과. 문서 전용 후속 변경으로 Python 테스트는 재실행하지 않음 |
| CHG-0032 | 2026-08-19 16:16 | 복수 사용자 인증 | Tailscale 로그인 허용값을 쉼표 구분 복수 목록으로 확장하고 기존 단일 설정을 호환하며, 운영 검증·감사·자동시작 검사가 모든 허용 사용자를 인식하도록 변경. 다른 비밀값을 보존하면서 허용 목록만 갱신하는 무출력 관리 스크립트, SSH 인수 분리 없이 대시보드 작업만 재시작하는 스크립트와 대표이사·유지보수 담당자 계정·승인 기기의 이중 통제 원칙을 기획·배포 문서에 반영 | `app/config.py`, `app/security.py`, `.env.example`, `scripts/set_tailscale_allowed_users.ps1`, `scripts/restart_dashboard_task.ps1`, `scripts/verify_delivery.ps1`, `scripts/install_startup_tasks.ps1`, `scripts/audit_delivery_readiness.ps1`, `tests/test_api.py`, `tests/test_config.py`, 기획서·배포 체크리스트·서버 인수인계·유지보수 런북·진행 현황, 서버 운영 `.env`·대시보드 작업 | 복수 두 계정 허용·미등록 계정 403·대소문자 정규화·기존 단일 설정 병합 회귀를 포함해 전체 50개 테스트 통과. Python compileall, PowerShell 9개 파일 구문, 임시 `.env`의 중복 제거·소문자 정규화·단일 설정행 갱신, `git diff --check` 통과. 첫 샌드박스 테스트는 기존 가상환경 원본 Python 경로 접근 불가로 실패해 실제 사용자 환경에서 재실행 후 통과. 원격 `schtasks.exe`와 와일드카드 종료 시도는 작업명 공백·정확 일치 문제로 실행 전 실패해 작업 상태가 바뀌지 않았고, 전용 재시작 스크립트로 정상 재시작. 운영 커밋 `4f3e183`, 제한 ACL 재적용, 설정된 두 계정 모두 인증·실데이터 87건·공유 색인·활성 메일 1계정 검증 통과, 유지보수 담당자 비공개 HTTPS 200 확인. 대표이사 기기 실제 브라우저 확인은 대기 |
| CHG-0033 | 2026-08-19 16:33 | 세 번째 허용 계정 추가 | 기존 유지보수 담당자·대표이사 계정을 유지하면서 초대된 외부 Gmail 기반 Tailscale 계정을 서버 허용 목록에 추가하고 제한 ACL 재적용·대시보드 작업 재시작 | 서버 운영 `.env`·대시보드 작업, 진행 현황 | 허용 계정 수 3개로 갱신하고 비밀값·실제 로그인 식별자를 출력하거나 문서화하지 않음. 전용 재시작 스크립트 정상 완료 후 세 계정 모두 인증, 실데이터 87건·공유 색인·활성 메일 1계정 운영 검증 통과. 초대 수락 여부와 해당 기기의 실제 브라우저 접속은 미검증 |
| CHG-0034 | 2026-08-20 18:01 | 추가 기획 | 대표이사 주간 현황판을 기존 백오피스에 통합하고 NAS 원본을 최근 파일의 정본으로 사용하는 방향을 확정. File Station API 최소 권한 파일럿, 제한 문서 ID, 독립 캐시·실패 경계, 일정·할 일 후속 단계와 `yjs_data_platform` MCP 인수 조건을 문서화 | `대표이사_주간현황_NAS연동_기획서.md`, `README.md`, 진행 현황 | Markdown 링크 대상·코드 펜스·`git diff --check` 검증. 문서 전용 변경으로 Python 테스트는 미실행 |
| CHG-0035 | 2026-08-25 15:29 | NAS 실연동·읽기 전용 기반 | 관리자 자격증명 없이 File Station API의 실제 읽기 계약과 파일 1개 제한 다운로드를 검증하고 로컬 NAS 루트 설정을 보정. HTTPS·고정 루트·다운로드 상한·세션 만료 1회 재인증을 적용한 읽기 전용 클라이언트와 설정 검증을 추가했으며 업로드·삭제·이동 API는 구현하지 않음. 계정이 대상 폴더 밖 일부 목록도 읽는 최소 권한 미충족 사실을 차단 사항으로 유지 | `.env`(로컬·Git 제외), `.env.example`, `app/config.py`, `app/nas.py`, `tests/test_config.py`, `tests/test_nas.py`, 진행 현황 | 실제 NAS API 탐색·읽기 전용 로그인·고정 루트 목록·20,637바이트 다운로드 크기 일치·임시 파일 삭제·로그아웃 통과. 관리자 계정 미사용·NAS 쓰기 0건. 합성 테스트를 포함한 전체 56개 통과, `compileall` 통과, `git diff --check` 통과. NAS·Drive 표본 비교, 형식별 미리보기, 워커·문서 ID·API·화면 통합은 미실행 |
| CHG-0036 | 2026-08-26 09:42 | NAS 백엔드 수직 슬라이스 | 최근 파일 공급자, 내부 경로를 숨기는 현재 캐시 문서 ID 레지스트리, 60초 독립 워커·마지막 성공 캐시, 최근 파일·첨부 다운로드 API와 NAS 독립 health를 구현. NAS 실패가 기존 대시보드에 전파되지 않고 조작 ID·크기 초과·원시 오류를 일반화해 거부하도록 보완했으며 로컬 NAS 기능을 활성화 | `.env`(로컬·Git 제외), `app/domain.py`, `app/main.py`, `app/nas_service.py`, `app/nas_runtime.py`, `tests/test_api.py`, `tests/test_nas_service.py`, `tests/test_nas_runtime.py`, 진행 현황 | 합성 집중 테스트 28개와 전체 62개 통과, `compileall`·`git diff --check` 통과. 실제 읽기 전용 NAS 공급자에서 최근 10건·15,872바이트 다운로드 크기 일치·공개 경로 0건 확인. 실제 FastAPI `/api/nas/recent`·문서 다운로드 200, 크기 일치, NAS health `ok` 확인. NAS 쓰기 0건·관리자 계정 미사용. 첫 실행 명령의 인용 오류 1건은 NAS 연결 전에 실패했고 구문 수정 후 통과. Drive 비교·미리보기·UI·운영 서버 반영은 미실행 |
| CHG-0037 | 2026-08-26 16:33 | NAS 주간 현황 화면 | 권장안에 따라 기존 경영 화면과 분리된 `주간 현황` 탭을 만들고 NAS 최근 파일의 이름·형식·크기·수정 시각·첨부 다운로드를 연결. 해시·키보드 탭 전환, 60초 독립 갱신, 로딩·비활성·빈 목록·stale·연결 실패·404·413·503 안내를 추가하고 Drive 비교·인라인 미리보기·원천 미확정 일정과 할 일은 제외 | `경영대시보드_예시화면_v2.html`, `tests/test_api.py`, 진행 현황 | 관련 테스트 19개와 전체 62개 통과, Python `compileall`, JavaScript 구문, HTML 고유 ID 57개·중복 0개, `git diff --check` 통과. 실제 `.env` 로컬 앱에서 화면 200·주간 탭/호출 코드 포함·NAS enabled·stale=false·최근 10건·내부 경로 비노출·NAS health `ok` 확인 후 서버 정상 종료. 연결 가능한 앱 내 브라우저 목록이 비어 있어 데스크톱·모바일 시각·클릭 검증은 미실행으로 정확히 유지하고 다른 자동화 수단으로 우회하지 않음. NAS 쓰기·운영 서버 배포는 미실행 |
| CHG-0038 | 2026-09-01 10:30 | NAS UDP 활동 피드 전환 | File Station 최근 파일·다운로드 화면을 의미 추론 없는 UDP 514 `NAS 최근 활동` 피드로 교체. 별도 `NAS_LOG_*` 설정, `recvfrom()` 허용 송신자 검사, 크기·strict UTF-8·제어문자 방어, 직접 action/item 표식과 단일 안전 경로만 추출하는 순수 파서, 기존 SQLite 영속 저장·canonical 중복 제거·30일 보존, 독립 수신 runtime·실패 격리·`nas_activity` health, `/api/nas/activity`, 참고용 안내와 unknown/대상 없음 UI를 구현. 기존 File Station 클라이언트·워커·`/api/nas/recent`·문서 다운로드 API·테스트는 유지하고 UI 호출만 제거. 기획서·NAS 전환 문서·README·환경 예시를 현행화 | `.env.example`, `README.md`, `app/config.py`, `app/domain.py`, `app/main.py`, `app/nas_activity.py`, `app/nas_activity_runtime.py`, `app/state_store.py`, `tests/test_api.py`, `tests/test_config.py`, `tests/test_nas_activity.py`, `tests/test_nas_activity_runtime.py`, `tests/test_state_store.py`, `경영대시보드_예시화면_v2.html`, `경영대시보드_기획서.md`, `대표이사_주간현황_NAS연동_기획서.md`, 진행 현황 | 신규 집중 테스트 38개와 기존 File Station 회귀를 포함한 전체 77개 통과, Python `compileall`, JavaScript 구문, HTML 고유 ID 57개·중복 0개, 민감 필드·UI 엔드포인트·`recvfrom()` 정적 계약, `git diff --check` 통과. 루프백 실제 UDP 514에서 합성 1건을 원문 출력 없이 수신·정규화·메모리 SQLite 저장해 `stored=1` 확인하고 포트 해제. 첫 `python` 명령은 별칭 부재, 저장소 가상환경은 원본 경로 인코딩 문제로 실패해 설치된 Python 3.11로 재검증. 첫 실제 UDP 명령은 인용 구문 오류로 실행 전 실패, 다음 시도는 수신 후 Windows 임시 백업 삭제 잠금으로 성공 판정 전 실패했으며 전용 임시 디렉터리를 안전하게 제거한 뒤 메모리 SQLite 방식으로 최종 통과. 정적 `rg` 1회는 인용 오류 후 안전한 패턴으로 재실행 통과. 실제 NAS 송신·브라우저 시각·운영 `.env`·방화벽·배포는 미실행. NAS 관리자 계정·NAS 쓰기·커밋·push·운영 변경 0건 |
