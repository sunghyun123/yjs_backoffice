# 대표이사 경영 현황·NAS 최근 활동 기획서

> 최초 작성: 2026-08-20 KST
>
> 현행 전환: 2026-09-01 KST
>
> 상태: File Station 파일 파일럿에서 의미 추론 없는 UDP 활동 피드로 전환 구현·검증 중
>
> 구현 저장소: `yjs_backoffice`

## 1. 현행 결정 요약

별도 `주간 현황` 탭은 사용하지 않는다. `경영 현황` 한 화면에서 `씽크와이즈 최근 변경`과 `NAS 최근 활동`을 같은 목록 형식으로 나란히 보여준다. NAS 영역은 더 이상 File Station API로 전체 하위 폴더를 순회해 최근 파일과 다운로드를 제공하지 않고, Synology NAS가 UDP 514로 보낸 로그 중 실제 수신된 범위에서 안전하게 정규화할 수 있는 최소 필드만 사용한다.

이 기능의 제품 계약은 다음과 같다.

- 생성·수정·이동·삭제를 정확히 판정하려 하지 않는다.
- 로그에 직접 `rename` 토큰이 있으면 `rename`으로만 표시한다. 실제 저장·이동·수정으로 재해석하지 않는다.
- 직접 확인된 제한 action 토큰이 없으면 `unknown`으로 저장하고 화면에는 `NAS 작업 감지`로 표시한다.
- 대상 경로를 설정 범위 아래로 안전하게 상대화하지 못하면 대상 필드를 비우고 `대상 정보 없음`으로 표시한다.
- 이 기능은 수신된 범위의 참고용 피드이며 감사 원장이 아니다.
- 원문 로그, 송신자 IP, NAS·클라이언트 IP, 호스트명, 프로토콜, 절대 경로는 저장·로그·API에 남기지 않는다.

기존 File Station 클라이언트, 최근 파일 API, 불투명 문서 ID와 제한 다운로드 API는 이번 전환에서 삭제하지 않는다. 새 활동 피드가 충분히 운영 검증된 뒤 별도 승인으로 제거 여부를 결정한다.

## 2. 전환 근거

### 첫 15분 익명 수신

- 전체 20패킷 중 허용 NAS 송신자 19패킷, 비허용 로컬 합성 1패킷이었고 로컬 패킷은 정상 차단됐다.
- 해석 가능한 작업형 이벤트는 3건, 해석 실패는 16건이었다.
- 직접 확인된 action은 `rename` 3건뿐이고 `create`·`modify`·`move`·`delete`는 0건이었다.
- 시각·작업자 표식은 19건, 파일/폴더 표식은 5건, 경로·작업 표식은 3건이었다.
- malformed UTF-8과 과대 패킷은 없었다.

### 사용자 작업 5종을 포함한 두 번째 수신

사용자가 파일 생성, 내용 수정·저장, 파일명 변경, 폴더 이동, 파일 삭제를 직접 수행했다. 수신 로그에서 확인 가능한 작업형 이벤트는 3건이었고 모두 `rename`이었다. 시각·작업자·대상·경로 표식은 있었지만 실제 다섯 작업의 의미를 구분할 수 없었다.

따라서 action 의미 분류는 포기하고, 직접 확인된 토큰과 안전한 표시 필드만 참고용으로 제공하는 방향을 승인했다. 두 번째 수신기는 사용자 완료 직후 종료 과정에서 최종 집계 출력이 생략됐지만 UDP 514 해제는 확인했다.

## 3. 승인 구조

```text
Synology NAS 로그
  -> UDP 514 독립 수신기
  -> 허용 송신자·크기·UTF-8 검사
  -> 최소 필드 추출·민감정보 제거·중복 제거
  -> 기존 dashboard SQLite에 최근 활동 저장
  -> GET /api/nas/activity
  -> 경영 현황의 NAS 최근 활동 피드
```

브라우저는 UDP에 접근하지 않는다. FastAPI 프로세스의 독립 수신기만 UDP를 받고 브라우저는 기존 Tailscale Serve 비공개 HTTPS를 통해 정규화된 API만 읽는다.

## 4. 수신기와 설정 계약

수신기는 `recv()`가 아니라 `recvfrom()`을 사용해 송신자 IPv4를 먼저 확인한다.

| 설정 | 기본값 | 계약 |
|---|---:|---|
| `NAS_LOG_ENABLED` | `false` | File Station의 `NAS_ENABLED`와 별개인 활동 수신 플래그 |
| `NAS_LOG_BIND_ADDRESS` | `0.0.0.0` | UDP 수신 바인딩 주소 |
| `NAS_LOG_PORT` | `514` | UDP 수신 포트 |
| `NAS_LOG_ALLOWED_SOURCE_IPS` | 빈 값 | 활성화 시 필수인 쉼표 구분 NAS IPv4 허용 목록 |
| `NAS_LOG_TARGET_ROOT` | 빈 값 | 상대 경로 계산을 허용할 NAS 대상 루트, 활성화 시 필수 |
| `NAS_LOG_RETENTION_DAYS` | `30` | SQLite 활동 보존 기간 |
| `NAS_LOG_MAX_DATAGRAM_BYTES` | `8192` | 초과 패킷 폐기 상한 |

- 허용 목록에 없는 송신자는 내용 해석 전에 폐기한다.
- 수신 버퍼는 최대 크기보다 1바이트 크게 잡아 잘린 과대 datagram도 초과로 판정한다.
- UTF-8은 `errors="strict"`로 해석하며 malformed 데이터는 폐기한다.
- NUL과 제어문자가 있는 메시지는 폐기한다.
- 원문 byte와 송신자 주소는 한 datagram 처리 범위를 넘겨 보관하지 않는다.
- 포트 충돌·권한·소켓 오류는 `nas_activity` 상태만 degraded로 만들고 씽크와이즈·메일·기존 대시보드 시작을 중단시키지 않는다.

운영 배포 전 Windows 방화벽의 UDP 514 인바운드는 실제 NAS IPv4로만 제한한다. 공유기 포트포워딩, 공개 인터넷 노출, Tailscale Funnel은 사용하지 않는다.

## 5. 정규화 규칙

### action

직접 확인하는 제한 토큰은 `create`, `modify`, `move`, `delete`, `rename`이다. 완전한 토큰이 하나만 확인될 때만 `source_action`에 보존한다. `saved`, `renamed` 같은 다른 단어나 문맥을 제한 토큰으로 변환하지 않는다. 충돌하거나 확인할 수 없으면 `unknown`이다.

### 작업자와 대상 유형

- 작업자는 명시된 `user`·`username`·`actor`·`account` 표식에서만 추출한다.
- IP 형태의 작업자 값, 충돌하는 복수 값, 제어문자·과도하게 긴 값은 저장하지 않는다.
- `file`과 `folder` 표식이 하나만 직접 확인될 때만 해당 유형을 저장한다.
- 두 표식이 함께 있거나 확인할 수 없으면 `item_type=unknown`이다.
- 파일 확장자로 `file`을 추론하지 않는다.

### 경로

- 절대 경로 자체는 저장하지 않는다.
- 하나의 대상 경로가 설정된 `NAS_LOG_TARGET_ROOT` 아래임을 확인할 수 있을 때만 `relative_folder`와 `display_name`으로 분리한다.
- 범위 밖 경로, `..` 이동, 복수 경로, 상충 경로, 과도하게 긴 경로는 대상 필드를 모두 비운다.
- `rename` 로그에 이전·이후 경로가 함께 있으면 어느 쪽이 화면 대상인지 추론하지 않고 대상 정보를 비운다.
- 원문 일부를 대상 정보 대신 사용하지 않는다.

### 이벤트 선택과 시각

- action·item·path 표식 중 하나라도 있어야 활동 후보로 저장한다.
- 시각·작업자만 있는 heartbeat 등 주기성 이벤트는 피드 노이즈이므로 저장하지 않는다.
- 원문에서 ISO 또는 일반 syslog 시각을 안전하게 읽으면 `occurred_at`으로 사용한다.
- 원문 시각을 읽을 수 없으면 안전하게 알고 있는 수신 시각을 `occurred_at`으로 사용하고 `received_at`은 별도 보존한다.

## 6. SQLite 저장·중복·보존

기존 `data/dashboard.db`에 다음 정규화 필드만 둔다.

```text
event_id
occurred_at
source_action
actor
item_type
relative_folder
display_name
received_at
dedupe_key       # 내부 전용, API 비노출
```

원문 로그, NAS IP, 송신자 IP, 절대 경로 컬럼은 만들지 않는다. 정규화 필드의 canonical JSON을 SHA-256으로 계산한 `dedupe_key`에 고유 제약을 적용해 프로세스 재시작 뒤에도 반복 전송을 한 번만 저장한다.

보존 정리는 `received_at` 기준으로 `NAS_LOG_RETENTION_DAYS`가 지난 행만 삭제한다. 시작 시 한 번 실행하고 이후에는 최대 시간당 한 번만 실행해 패킷마다 무거운 정리가 일어나지 않게 한다.

## 7. API와 health 계약

### `GET /api/nas/activity`

초기 API는 `limit` 기반이며 기본 30, 최소 1, 최대 100이다. 응답에는 다음만 포함한다.

```text
enabled
listening
status
stale
fetched_at
last_received_at
notice = "수신된 로그 기준 참고용 · 모든 NAS 작업을 보장하지 않음"
error             # 일반화된 사용자 안내만
items[]           # 정규화 필드만
```

원문, IP, 호스트명, 프로토콜, 절대 경로, 자격증명, `dedupe_key`는 반환하지 않는다. 기존 인증, `Cache-Control: no-store`, CORS 비개방 정책을 그대로 적용한다.

### `/api/health`의 `nas_activity`

```text
status / enabled / listening
received_count / stored_count / discarded_count / duplicate_count
last_received_at / last_error_at / last_error_type
```

오류 메시지 원문은 넣지 않고 예외 타입만 기록한다. `nas_activity`가 degraded여도 기존 전체 `status`를 강제로 degraded로 바꾸지 않는다. 기존 File Station 상태인 `nas`와 새 `nas_activity`를 섞지 않는다.

## 8. 화면 계약

별도 `주간 현황` 탭을 제거하고 `경영 현황` 안의 `씽크와이즈 최근 변경` 옆에 `NAS 최근 활동`을 배치한다. 두 피드는 동일하게 `작업 배지 / 대상과 메타 / 시각` 행을 사용하며 각각 최근 6건을 표시한다.

```text
rename  주간업무계획 / 상대 위치 / 표시명  14:32
        사용자 · 폴더
```

- action이 `unknown`이면 `NAS 작업 감지`로 표시한다.
- 작업자가 없으면 `작업자 미확인`, 대상이 없으면 `대상 정보 없음`으로 표시한다.
- `수신된 로그 기준 참고용 · 모든 NAS 작업을 보장하지 않음`을 항상 표시한다.
- UDP 유실·중복 가능성, 수신 시작 전 기록 부재, 감사 원장이 아님을 명시한다.
- 로딩·비활성·빈 목록·수신기 오류·활동 API 연결 실패를 구분한다.
- 모든 동적 문자열은 `textContent`로 출력한다.
- UI는 `/api/nas/recent`와 `/api/nas/documents/{document_id}`를 호출하지 않는다.

## 9. 기존 File Station 파일럿 검증 이력과 전환 판단

이 이력은 삭제하지 않고 UDP 전환의 근거와 롤백 판단 자료로 유지한다.

| 시점 | 기존 파일럿 결과 | 현재 판단 |
|---|---|---|
| 2026-08-25 | 비관리자 계정으로 공식 File Station API 탐색·로그인·대상 폴더 목록·20,637바이트 파일 1개 제한 다운로드·크기 일치·로그아웃 검증. 관리자 계정 미사용, NAS 쓰기 0건, 전체 56개 테스트 통과 | 읽기 전용 클라이언트 자체의 안전성 근거로 보존 |
| 2026-08-26 오전 | 전체 하위 폴더 최근 파일 공급자, 불투명 문서 ID, 60초 독립 워커, 마지막 성공 캐시, `/api/nas/recent`, 제한 다운로드와 `nas` health 구현. 전체 62개 테스트와 실제 15,872바이트 다운로드·경로 비노출 확인 | 백엔드 코드와 API는 유지하되 현재 UI에서는 사용하지 않음 |
| 2026-08-26 오후 | 별도 `주간 현황` 탭에 최근 파일 10건과 다운로드 버튼, 로딩·비활성·빈 목록·stale·오류 상태를 연결. 전체 62개 테스트·JS·고유 ID·diff 검사 통과 | 화면 계약은 UDP 활동 피드로 대체 |
| 권한 검토 | 서비스 계정 쓰기는 거부됐지만 대상 폴더 밖 일부 목록이 보여 계정 수준 최소 권한은 미충족 | File Station 운영 확대를 중단한 근거로 유지 |

File Station 구현에는 업로드·삭제·이동 메서드가 없고, 기존 API도 Tailscale 인증과 no-store 정책을 유지한다. 향후 제거 전에는 새 활동 피드 운영 안정성, 실제 사용자 화면, 롤백 필요성을 별도 검토한다.

## 10. 검증 기준

### 보안

- 비허용 송신자, malformed UTF-8, 과대 datagram, 제어문자가 저장되지 않는다.
- 원문 로그와 송신자 IP가 SQLite·로그·API·health에 없다.
- 절대 경로가 SQLite 표시 필드·API·HTML 오류 안내에 없다.
- 실제 NAS 쓰기와 관리자 계정 사용은 0건이다.

### 의미 보존

- `rename`을 `move`, `modify`, `create` 등으로 바꾸지 않는다.
- 직접 확인하지 않은 action은 `unknown`이다.
- file/folder 충돌은 `unknown`이다.
- 안전하게 상대화하지 못한 대상은 비어 있다.
- 시각·작업자만 있는 주기성 이벤트는 저장하지 않는다.

### 내구성과 격리

- 중복 이벤트는 한 번만 저장되고 재시작 뒤에도 유지된다.
- 보존 기간이 지난 행만 제한 주기로 정리된다.
- UDP 514 충돌과 수신 오류가 씽크와이즈·메일·대시보드 시작을 중단시키지 않는다.
- 활동 API와 health가 일반화된 상태만 제공한다.
- 기존 File Station 코드·API와 전체 회귀 테스트가 유지된다.

## 11. 배포·롤백

이번 구현 작업에서는 커밋·push·운영 배포·방화벽 변경을 하지 않는다.

운영 배포 전 별도 승인 아래 다음을 확인한다.

1. UDP 514가 비어 있는지 확인한다.
2. Windows 방화벽 인바운드를 실제 NAS IPv4로만 제한한다.
3. `NAS_LOG_ALLOWED_SOURCE_IPS`와 방화벽 허용 주소가 일치하는지 값 노출 없이 확인한다.
4. 예약 작업 재시작 뒤 수신기 listening과 기존 HTTPS 화면을 확인한다.
5. 포트 충돌 시 활동 수신만 비활성화하고 기존 대시보드는 유지한다.

활동 UI에 문제가 있으면 `NAS_LOG_ENABLED=false`로 수신을 끄고 경영 현황의 NAS 활동 카드만 비활성 상태로 둘 수 있다. 씽크와이즈·메일·프로젝트 영역은 유지한다. 기존 File Station 화면으로 자동 복귀하지 않으며, 복귀가 필요하면 별도 검토·승인을 거친다.

## 12. 제외 범위

- NAS 작업 의미의 정확한 생성·수정·이동·삭제 판정
- 감사 원장 또는 규정 준수 로그 보장
- 원문 로그 저장·검색·다운로드
- NAS 전체 공유 폴더 색인
- NAS 파일 생성·수정·이동·삭제
- 관리자 계정, SSH 파일 접근, 광범위 방화벽 공개
- Drive 매핑, Universal Search, MCP, AI 자동 요약
