# CLAUDE.md — (이 레포 전용 / 프로젝트 스텁)

> 성장 워크플로우(5게이트·티어·캘리브레이션)와 **보강 로그(성장 상태) 정본은 전역 메모리
> `~/.claude/CLAUDE.md`에 있다.** 그 규칙을 그대로 따른다. 여기엔 성장 상태를 두지 않는다.
> 이 파일은 *이 레포에만 해당하는 정보*만 담는다.

---

## 이 프로젝트 컨텍스트

- **프로젝트명:** yjs_backoffice (경영지원 대시보드)
- **한 줄 목적:** 대표이사가 씽크와이즈 협업 현황과 최근 안 읽은 메일을 한 화면에서 확인하는 전용 대시보드
- **스택:** Python 3.11+ / FastAPI / uvicorn / PyMySQL / SQLite / 단일 HTML·바닐라 JS
- **실행/배포:** 사내 Windows 서버 + 작업 스케줄러 + Tailscale Serve 비공개 HTTPS
- **기획 정본:** `경영대시보드_기획서.md`
- **개발 진행 정본:** `경영대시보드_개발진행현황.md`
- **승인 목업:** `경영대시보드_예시화면_v2.html`

## 이 레포의 지뢰 / 주의 (게이트에서 우선 캐물을 것)

- **인사·급여 데이터를 다루게 되면 인가는 무조건 티어 A.** 경영지원은 성격상 "볼 수 있는 사람"이
  업무별로 갈린다 → RLS(전역 §1 지뢰 1번)를 이 레포에서 다시 만난다.
- **회계 금액이 들어오면 정합성 로직은 티어 A.** yjs_erp에서 겪은 파생값 저장(달성률 127.19%)·
  준공액 정정 사고가 여기서도 같은 모양으로 재발할 수 있다.
-

## 이 레포 특유의 규칙 / 관례

- 날짜를 다루게 되면 **서버 시계를 믿지 말고 KST를 명시**한다 (전역 §1 타임존 항목).
- 씽크와이즈 MariaDB는 **SELECT 전용**이다. INSERT / UPDATE / DELETE / DDL을 절대 실행하지 않는다.
- FastAPI와 백그라운드 워커는 단일 프로세스로 실행한다.
- 비밀값은 `.env`에만 두고 저장소·로그·진행 문서에 기록하지 않는다.
-

## 개발 진행 자동 기록 — 필수

소스·설정·테스트·문서를 변경하는 모든 작업은 종료 전에 반드시 `경영대시보드_개발진행현황.md`를 같은 작업 안에서 갱신한다.

1. 상단의 마지막 갱신 시각, 현재 단계, 다음 작업을 갱신한다.
2. 해당 Phase의 상태와 완료 조건을 실제 결과에 맞게 갱신한다.
3. 변경 기록에 KST 시각, 변경 목적, 영향 파일, 실행한 검증, 결과를 한 행 추가한다.
4. 검증하지 않은 항목은 `미실행`이라고 적고 완료로 표시하지 않는다.
5. 실패·차단도 숨기지 말고 차단 사항에 기록한다.
6. 읽기 전용 조사처럼 파일 변경이 전혀 없는 작업은 변경 기록을 추가하지 않는다.
7. 여러 파일이 하나의 목적을 위한 변경이면 한 건으로 묶고, 목적이 다르면 기록을 분리한다.

진행 문서 갱신 누락은 작업 미완료로 간주한다. 사용자에게 완료를 보고하기 전에 반드시 문서가 갱신되었는지 확인한다.

## 아키텍처 지도 (⚠️ 내가 직접 손으로 그린다 — Claude가 채우지 않는다)

> 이건 내 머릿속 멘탈 맵의 종이 버전이다. Claude가 채워주면 머리에 안 남는다.
> 전역 §1.5 "구조 장악"의 이 레포 실물. **주 1회: 안 보고 다시 그려본 뒤 → 실제 코드와 대조(diff)해 틀린 곳을 고친다.**
> (예측 적중률 로그는 여기 말고 전역 `~/.claude/CLAUDE.md` §1.5에 쌓는다.)

- **진입점(들):**
- **핵심 모듈과 책임:**
  - `___` → (무슨 일을 하나)
  - `___` →
- **데이터 흐름 (요청 1개가 지나가는 길):**
  1. 사용자가 ___ 하면 →
  2. `___` 파일의 `___` 함수 →
  3. DB / 외부 API `___` →
  4. 응답 `___`
- **핵심 로직이 사는 곳:**
- **아직 지도에서 깜깜한 구역:** (전부 — 아직 코드가 없다)
