# 경영 대시보드 서버 유지보수·배포

이 문서는 최초 설치가 끝난 현재 운영 서버에 이후 코드 변경을 안전하게 반영하기 위한 실행 절차다. 최초 설치·Grants·재부팅 인수 시험은 `배포_체크리스트.md`를 따른다.

## 1. 현재 운영 기준

| 항목 | 현재 값 |
|---|---|
| 개발 저장소 | `C:\Users\조성현\Documents\GitHub\yjs_backoffice` |
| 게시 브랜치 | `agent/management-dashboard-delivery` |
| 서버 SSH | `user@192.168.0.76` |
| 서버 대시보드 경로 | `C:\apps\yjs_backoffice` |
| 서버 위키 경로 | `C:\apps\thinkwise-wiki` |
| 대시보드 작업 | `YJS Management Dashboard` |
| 공유 색인 작업 | `YJS ThinkWise Shared Index Sync` |
| 비공개 주소 | `https://yj-dashboard.tail7cbfd2.ts.net/` |

- 서버 저장소는 읽기 전용 GitHub 배포 키로 원격 코드를 받는다. 개인 키 원문은 저장소나 문서에 기록하지 않는다.
- SSH 비밀번호, 메일 주소, DB 비밀번호, Tailscale 로그인은 이 문서에 기록하지 않는다. 운영 비밀값은 서버의 `.env`에만 둔다.
- 서버 IP나 브랜치가 바뀌면 이 표와 `경영대시보드_개발진행현황.md`를 함께 갱신한다.

## 2. 절대 지킬 원칙

- 씽크와이즈 MariaDB에는 `SELECT`만 실행한다. `INSERT`, `UPDATE`, `DELETE`, DDL을 금지한다.
- 서버의 `.env`, `data`, 위키 색인과 백업을 Git으로 덮어쓰거나 삭제하지 않는다.
- 서버 작업 트리에 미커밋 변경이 있으면 `pull`, 초기화, 강제 덮어쓰기를 중단하고 먼저 원인을 확인한다.
- 대시보드는 계속 `127.0.0.1:8080`에만 바인딩한다. LAN 공개, 포트포워딩, Tailscale Funnel을 사용하지 않는다.
- 원격 PowerShell `-EncodedCommand`를 사용하지 않는다. 네이티브 명령을 직접 호출하거나 저장소의 검토된 `.ps1`을 평문 `-File`로 실행한다.
- 일반 코드 배포에는 서버 재부팅이 필요 없다. 재부팅 시험은 사용자가 승인한 시간에만 한다.
- 기존 `ThinkwiseWiki` 작업은 유지하고, 비활성화된 기존 `ThinkwiseWikiSync`를 임의로 다시 켜지 않는다.

## 3. 평소 코드 수정과 게시

개발 PC에서 변경 전 작업 트리를 확인한다.

```powershell
Set-Location -LiteralPath "C:\Users\조성현\Documents\GitHub\yjs_backoffice"
git status --short
git branch --show-current
```

소스·설정·테스트·문서를 변경하면 같은 작업에서 `경영대시보드_개발진행현황.md`도 갱신한다. 변경에 맞는 테스트를 실행하고 최소한 다음을 확인한다.

```powershell
.\.venv\Scripts\python.exe -m pytest
git diff --check
```

검증한 파일만 명시적으로 커밋하고 현재 운영 브랜치에 게시한다. 사용자 참고 이미지나 `.env`는 포함하지 않는다.

```powershell
git add -- <검증한 파일 목록>
git commit -m "<변경 목적>"
git push origin agent/management-dashboard-delivery
```

## 4. 서버에 바로 반영

개발 PC에서 아래 순서로 실행한다. 첫 명령에 출력이 있으면 서버 작업 트리가 깨끗하지 않은 것이므로 배포를 중단한다.

```powershell
ssh user@192.168.0.76 git -C C:\apps\yjs_backoffice status --short
ssh user@192.168.0.76 git -C C:\apps\yjs_backoffice branch --show-current
ssh user@192.168.0.76 git -C C:\apps\yjs_backoffice pull --ff-only
ssh user@192.168.0.76 git -C C:\apps\yjs_backoffice rev-parse --short HEAD
git rev-parse --short HEAD
```

서버 브랜치는 `agent/management-dashboard-delivery`여야 하고 마지막 두 커밋 값이 같아야 한다. `pull --ff-only`가 거부되면 강제 초기화하지 말고 서버 상태를 조사한다.

### 변경 종류별 재시작

| 변경 | 추가 조치 |
|---|---|
| HTML·CSS·JavaScript·문서만 변경 | 재시작 없음. 브라우저에서 강력 새로고침 |
| `app/`, `run.py`, 대시보드 Python 변경 | 대시보드 예약 작업만 종료 후 실행 |
| `requirements.txt` 변경 | 두 저장소 요구사항 설치 후 관련 작업 재시작 |
| 위키 동기화 코드 변경 | 위키 저장소도 별도 검증·배포 후 공유 색인 작업 재시작 |
| 서버 `.env` 변경 | ACL 재적용 후 대시보드 작업 재시작 |
| Tailscale 설정 변경 | Serve/Funnel 상태 재검증. 재부팅은 불필요 |

재시작이 필요하면 서버에 SSH로 들어간 뒤 다음 네이티브 명령을 사용한다.

```powershell
schtasks.exe /End /TN "YJS Management Dashboard"
schtasks.exe /Run /TN "YJS Management Dashboard"

schtasks.exe /End /TN "YJS ThinkWise Shared Index Sync"
schtasks.exe /Run /TN "YJS ThinkWise Shared Index Sync"
```

의존성 변경 시 서버에서 사용하는 하나의 가상환경에 두 저장소 요구사항을 함께 설치한다.

```powershell
C:\apps\yjs_backoffice\.venv\Scripts\python.exe -m pip install `
  -r C:\apps\yjs_backoffice\requirements.txt `
  -r C:\apps\thinkwise-wiki\requirements.txt
```

허용 Tailscale 계정을 바꿀 때는 다른 비밀값을 건드리지 않는 전용 스크립트에 전체 허용 목록을 쉼표로 전달한다. 스크립트는 계정 값 자체를 출력하지 않는다. 실행 후 ACL을 재적용하고 대시보드 작업만 재시작한다.

```powershell
.\scripts\set_tailscale_allowed_users.ps1 -UsersCsv "<유지보수 로그인>,<대표이사 로그인>"
.\scripts\secure_runtime_acl.ps1 -WikiRoot C:\apps\thinkwise-wiki
schtasks.exe /End /TN "YJS Management Dashboard"
schtasks.exe /Run /TN "YJS Management Dashboard"
```

## 5. 배포 후 검증

보안 제품의 파일리스 탐지를 피하기 위해 인코딩 명령을 만들지 말고 검토된 파일을 그대로 실행한다.

```powershell
ssh user@192.168.0.76 powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File C:\apps\yjs_backoffice\scripts\verify_delivery.ps1 -RequireProduction
ssh user@192.168.0.76 C:\Progra~1\Tailscale\tailscale.exe serve status
ssh user@192.168.0.76 C:\Progra~1\Tailscale\tailscale.exe funnel status
ssh user@192.168.0.76 curl.exe -sS -o NUL -w "%{http_code}" https://yj-dashboard.tail7cbfd2.ts.net/api/health
```

정상 기준:

- 운영 검증 스크립트 통과
- HTTPS 상태 API `200`
- Serve 상태에 `tailnet only`와 `127.0.0.1:8080` 표시
- Funnel 상태에 `Available on the internet` 표시 없음
- 허용된 Tailscale PC에서 실제 화면과 변경 부분 확인
- `경영대시보드_개발진행현황.md`에 배포 커밋·검증 결과 기록

## 6. 실패 시 중단과 복구

- `git pull` 실패: 서버 `git status --short`, 현재 브랜치와 원격 상태를 확인하고 사용자 변경을 보존한다.
- 대시보드만 실패: `YJS Management Dashboard` 작업 상태와 `verify_delivery.ps1` 결과를 확인한다. 위키 웹 작업은 건드리지 않는다.
- 공유 색인이 오래됨: `YJS ThinkWise Shared Index Sync` 작업을 확인한다. MariaDB에 쓰기 쿼리를 실행하지 않는다.
- 잘못된 코드 배포: 로컬에서 문제 커밋을 `git revert <커밋>`으로 되돌리고 테스트·push한 뒤 서버에서 다시 `pull --ff-only`한다. `git reset --hard`로 서버를 강제 초기화하지 않는다.
- 비공개 주소가 열리지 않음: 접속 기기의 Tailscale 연결과 DNS를 먼저 확인하고, 서버의 Serve가 `tailnet only`인지 확인한다.
- `.env`나 상태 DB 복구가 필요하면 임의 수정하지 말고 `배포_체크리스트.md`의 ACL·백업 절차를 따른다.

## 7. Codex에 바로 요청하는 문장

다음 유지보수 세션에서는 아래처럼 요청하면 이 문서를 기준으로 배포까지 이어갈 수 있다.

> `AGENTS.md`와 `서버_유지보수_배포.md`, `경영대시보드_개발진행현황.md`를 읽고 수정 사항을 구현·검증한 뒤 운영 브랜치에 push하고 서버에 `pull --ff-only`로 반영해줘. 비밀값 출력, EncodedCommand, 재부팅, Funnel은 금지하고 진행 현황도 갱신해줘.
