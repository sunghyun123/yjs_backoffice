# 인수인계 — 경영 대시보드

이 문서 하나로 다음 세션이 재설명 없이 작업을 이어간다. 진행 상태의 정본은
`경영대시보드_개발진행현황.md`이고, 요구 정본은 `목표.html`이다. 이 문서는 그 둘을
어떻게 읽고 무엇부터 할지, 그리고 운영 서버에 어떻게 반영할지를 담는다.

비밀값(SSH 비밀번호, 메일 주소, DB 비밀번호, Tailscale 로그인, 배포 키)은 이 문서에
기록하지 않는다. 운영 비밀값은 서버의 `.env`에만 둔다.

---

## 1. 먼저 읽을 것

| 파일 | 성격 |
|---|---|
| `목표.html` | **요구 정본.** 대표이사가 직접 AI로 만든 화면. 이게 기준이다 |
| `경영대시보드_개발진행현황.md` | **진행 정본.** 상단 `다음 작업`과 `다음` 목록이 작업 순서다 |
| `경영대시보드_예시화면_v2.html` | 현재 구현 화면. 앱이 `/`로 그대로 서빙한다 |
| `경영대시보드_기획서.md` | 초기 기획. 요구 정본과 어긋나는 서술이 남아 있으니 정본을 우선한다 |
| `대표이사_주간현황_NAS연동_기획서.md` | **철회된 접근의 기록.** 되살리지 말 것 |

커밋 `04b43df`와 `a641720`의 본문에 직전 세션의 판단 근거가 들어 있다.

## 2. 확정된 결정 — 다시 논의하지 말 것

1. **NAS 연동은 철회했다.** UDP syslog 수신기와 File Station 파일럿을 모두 삭제했다.
   근거 ① 요구 정본에 작업자 표시가 없다 ② NAS 로그인 계정이 전원 공용 단일 ID라
   사용자명으로 작업자를 식별할 수 없다. 작업자를 알려면 IP가 담긴 로그가 필요한데
   그 경로는 확보하지 못했다. 되살리려면 처음부터 다시 짜야 한다(백업 없음).
2. **작업자(누가) 표시는 포기한다.** 사용자가 명시적으로 결정했다.
3. **구글 공유 드라이브 5TB 분할 마이그레이션은 기각했다.** Cloud Sync가 모든 변경을
   계정 하나로 뭉개서 작업자가 붕괴하고, 목표를 달성하지 못한다.
   Drive 연동의 대상은 공유 드라이브가 아니라 **대표이사 본인 계정**이다.
4. **화면 구성의 출처가 갈린다. 섞지 말 것.**
   - 대표이사 요청: 이번 주 일정 / Drive 최근 수정 파일 / 할 일 / 메일
   - 담당자 판단 추가분: 씽크와이즈 카드·차트 → 요구 정본 아래로 배치하고 나중에 확인받는다
5. 사용자의 장기 계획은 NAS·씽크와이즈·Drive를 MCP로 감싸는 것이다. 지금 대시보드는
   "사장님 개인용"으로 마무리한다. MCP는 씽크와이즈부터, 로컬 stdio로 시작하기로 정리됐다.

## 3. 요구 정본이 규정하는 것

- 섹션 3개, 순서대로: ① 이번 주 일정(Google Calendar) ② Drive 최근 수정 파일 ③ 할 일
- 파일 행은 4조각뿐: 아이콘 / 파일명 / 시각 / 열기 링크(새 탭, `drive.google.com`).
  폴더도 작업자도 없다. **단순한 게 요구다.**
- 통계 타일 4개 중 2개가 캘린더(이번 주 일정 건수, 가장 가까운 일정 D-n)
- `max-width: 720px`, 480px 브레이크포인트 = 모바일 우선.
  현재 구현은 넓은 데스크톱 레이아웃이라 미스매치다.
- 각주에 대표이사가 직접 적은 불편: "할 일 목록은 이 창을 닫으면 초기화돼요"

## 4. 다음 작업

진행현황 `다음` 목록의 2번부터 시작한다. 1번은 완료.

**2. 할 일 카드 추가 + 기존 `app/state_store.py`로 영속화**
대표이사가 직접 적은 불편을 고치는 것이라 만족도 대비 비용이 가장 좋다.
착수 전 게이트 1(설계 대안 2개)로 올릴 미결정 2건:
- **저장 범위** — 대표이사 1인용인가, Tailscale 계정별로 나뉘는가.
  현재 허용 계정은 3개인데 `state_store`는 단일 사용자 전제라 스키마가 갈린다.
- **완료 항목** — 체크하면 남기나 사라지나. 요구 정본은 진행률 바가 있어 완료분이
  분모에 남아야 하는데, 그러면 무한정 쌓인다.

**3. 구글 OAuth 1회로 Calendar + Drive 함께 연동** — 자격증명 미확보.
티어 A(처음 쓰는 인증 + 외부 API + 시크릿).

**4. 대표이사 주 사용 기기 확인 후 720px 모바일 정합**

**9. 씽크와이즈 카드를 아래로 내린 화면을 대표이사에게 보여주고 유지 여부 확인**

## 5. 개발 환경과 검증

```
저장소   C:\Users\조성현\Documents\GitHub\yjs_backoffice
브랜치   agent/management-dashboard-delivery
개발 PC  192.168.0.78
```

```powershell
.\.venv\Scripts\python.exe -m pytest -q    # 현재 50개 통과
git diff --check
```

- **실행 검증**: `TestClient`만으로는 JS 오류를 못 잡는다. uvicorn을 스레드로 띄우고
  실제 HTTP로 `/`, `/api/health`를 찍는 방식이 이 레포에서 잘 통했다.
- **JS 검증**: `<script>` 블록을 뽑아 `node --check`를 돌린다.
- **화면 편집 후**: markup의 `id`와 JS의 `getElementById`/`setText` 참조를 대조해
  고아 참조 0건을 확인한다.

## 6. 운영 서버 배포

```
서버 SSH        user@192.168.0.76   (DESKTOP-318VJ68, 개발 PC에서 키 인증됨)
대시보드 경로    C:\apps\yjs_backoffice
위키 경로        C:\apps\thinkwise-wiki
대시보드 작업    YJS Management Dashboard
공유 색인 작업   YJS ThinkWise Shared Index Sync
비공개 주소      https://yj-dashboard.tail7cbfd2.ts.net/
```

### 절대 지킬 원칙

- 씽크와이즈 MariaDB에는 `SELECT`만 실행한다. `INSERT`/`UPDATE`/`DELETE`/DDL 금지.
- 서버의 `.env`, `data`, 위키 색인과 백업을 Git으로 덮어쓰거나 삭제하지 않는다.
- 서버 작업 트리에 미커밋 변경이 있으면 `pull`·초기화·강제 덮어쓰기를 중단하고 원인부터 확인한다.
- 대시보드는 계속 `127.0.0.1:8080`에만 바인딩한다. LAN 공개·포트포워딩·Tailscale Funnel 금지.
- 원격 PowerShell `-EncodedCommand`를 쓰지 않는다(보안 제품의 파일리스 탐지에 걸린 이력 있음).
  네이티브 명령을 직접 호출하거나 저장소의 검토된 `.ps1`을 평문 `-File`로 실행한다.
- 일반 코드 배포에 재부팅은 필요 없다. 재부팅 시험은 사용자가 승인한 시간에만 한다.
- 기존 `ThinkwiseWiki` 작업은 유지하고, 비활성화된 `ThinkwiseWikiSync`를 임의로 켜지 않는다.

### 반영 순서

첫 명령에 출력이 있으면 서버 작업 트리가 깨끗하지 않은 것이므로 배포를 중단한다.

```powershell
git push origin agent/management-dashboard-delivery

ssh user@192.168.0.76 git -C C:\apps\yjs_backoffice status --short
ssh user@192.168.0.76 git -C C:\apps\yjs_backoffice branch --show-current
ssh user@192.168.0.76 git -C C:\apps\yjs_backoffice pull --ff-only
ssh user@192.168.0.76 git -C C:\apps\yjs_backoffice rev-parse --short HEAD
git rev-parse --short HEAD
```

마지막 두 커밋 값이 같아야 한다. `pull --ff-only`가 거부되면 강제 초기화하지 말고 조사한다.

### 변경 종류별 재시작

| 변경 | 조치 |
|---|---|
| HTML·CSS·JS·문서만 | 재시작 없음. 브라우저 강력 새로고침 |
| `app/`, `run.py` 등 Python | 대시보드 작업만 종료 후 실행 |
| `requirements.txt` | 두 저장소 요구사항 설치 후 관련 작업 재시작 |
| 위키 동기화 코드 | 위키 저장소도 별도 검증·배포 후 공유 색인 작업 재시작 |
| 서버 `.env` | ACL 재적용 후 대시보드 작업 재시작 |
| Tailscale 설정 | Serve/Funnel 상태 재검증. 재부팅 불필요 |

```powershell
schtasks.exe /End /TN "YJS Management Dashboard"
schtasks.exe /Run /TN "YJS Management Dashboard"
```

의존성 변경 시 서버의 단일 가상환경에 두 저장소 요구사항을 함께 설치한다.

```powershell
C:\apps\yjs_backoffice\.venv\Scripts\python.exe -m pip install -r C:\apps\yjs_backoffice\requirements.txt -r C:\apps\thinkwise-wiki\requirements.txt
```

허용 Tailscale 계정 변경, ACL 재적용, 대시보드 재시작 스크립트:

```powershell
.\scripts\set_tailscale_allowed_users.ps1 -UsersCsv "<허용 로그인 목록>"
.\scripts\secure_runtime_acl.ps1 -WikiRoot C:\apps\thinkwise-wiki
.\scripts\restart_dashboard_task.ps1
```

### 배포 후 검증

```powershell
ssh user@192.168.0.76 powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File C:\apps\yjs_backoffice\scripts\verify_delivery.ps1 -RequireProduction
ssh user@192.168.0.76 C:\Progra~1\Tailscale\tailscale.exe serve status
ssh user@192.168.0.76 C:\Progra~1\Tailscale\tailscale.exe funnel status
ssh user@192.168.0.76 curl.exe -sS -o NUL -w "%{http_code}" https://yj-dashboard.tail7cbfd2.ts.net/api/health
```

정상 기준: 검증 스크립트 통과 / 상태 API `200` / Serve에 `tailnet only`와 `127.0.0.1:8080` /
Funnel에 `Available on the internet` 없음 / 허용 PC에서 실제 화면 확인 / 진행현황에 결과 기록.

### 실패 시

- `pull` 실패: 서버 `git status --short`와 브랜치·원격을 확인하고 사용자 변경을 보존한다.
- 잘못된 배포: 로컬에서 `git revert <커밋>` 후 테스트·push하고 서버에서 다시 `pull --ff-only`.
  서버를 `git reset --hard`로 강제 초기화하지 않는다.
- 대시보드만 실패: `YJS Management Dashboard` 작업 상태와 `verify_delivery.ps1` 결과 확인.
  위키 웹 작업은 건드리지 않는다.
- 공유 색인이 오래됨: `YJS ThinkWise Shared Index Sync` 작업 확인. MariaDB에 쓰기 금지.
- 비공개 주소가 안 열림: 접속 기기의 Tailscale 연결·DNS 먼저, 그다음 서버 Serve가 `tailnet only`인지.
- `.env`·상태 DB 복구: 임의 수정하지 말고 `scripts/secure_runtime_acl.ps1`로 ACL을 재적용한다.
  일일 백업은 `data/backups/dashboard-YYYY-MM-DD.db`에 있고, 임시 SQLite로 열어
  `project_mark`·`setting`을 조회해 확인할 수 있다.

## 7. 함정

- **줄바꿈**: `core.autocrlf=true`인데 `.gitattributes`가 없다. 깨끗한 체크아웃에서 HTML이
  CRLF로 풀려 여러 줄 문자열 단언이 깨진다. `a641720`에서 한 군데 우회했으나 근본 처방
  (`.gitattributes`에 `* text=auto eol=lf`)은 **사용자 결정 대기 중**.
  HTML을 편집할 때는 줄바꿈을 보존한다(바이트 단위 편집 권장).
- **줄번호 편집**: 앞쪽을 먼저 지우면 뒤 번호가 밀린다. 직전 세션에서 이걸로 엘리먼트를
  하나 잘못 지웠다. 정확한 문자열 매칭으로 편집한다.
- **진행 기록**: 소스·설정·테스트·문서를 바꾸면 종료 전에 반드시
  `경영대시보드_개발진행현황.md`를 같은 작업에서 갱신한다(변경 기록 1행 추가 포함).
- **비밀값**: `.env`에 평문 비밀값이 있다(`.gitignore` 2행에 걸려 있음).
  로그·문서·커밋에 출력 금지.

## 8. 작업 방식

`~/.claude/CLAUDE.md`의 5게이트·티어 규칙을 그대로 따른다. 작업 시작 시 티어를 한 줄로 선언한다.
커밋 메시지는 원칙적으로 사용자가 쓴다(위임받은 경우에만 초안을 쓰고 그 사실을 밝힌다).
직전 세션에서는 디버깅이 중도에 방향 전환되어 `§1.5` 위치추적 로그에 남길 예측이 없었다.
