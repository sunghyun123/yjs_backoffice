# 경영 대시보드 구축 기획서

> **문서 용도 2가지**
> 1. 대표이사 보고용 — 1~5장. 개선 예시 화면은 `경영대시보드_예시화면_v2.html`.
> 2. 개발 지시서 — 6장 이하를 클로드 코드에 그대로 붙여넣어 구현합니다.
>
> 작성: 2026-08-14 / 작성자: 조성현 (DX 담당)
> **개정 3판 (2026-08-14 기술 검토·화면 개선안 반영)** — 상태 표시 규칙, 갱신 주기, 외부 HTTPS 접속안 확정

---

## 1. 배경과 목적

대표이사께서 흩어져 있는 사내 정보를 한 화면에서 파악하고자 하십니다. 현재는 씽크와이즈, 메일 3개 계정, 담당자 구두 보고로 정보가 나뉘어 있어 전체 상황을 한 번에 보기 어렵습니다.

**본 대시보드는 대표이사 전용 화면으로, 사내 서버에 상시 가동되며 허용된 기기에서 주소만 열면 최신 상태가 표시됩니다.** 씽크와이즈 데이터는 1분마다, 메일은 5분마다 자동 갱신합니다. 초기에는 사내에서 사용하고, 이후 같은 HTTPS 주소로 외부에서도 안전하게 접속할 수 있게 구성합니다.

---

## 2. 먼저 확인된 제약 (중요)

씽크와이즈 데이터베이스를 조사한 결과, **마인드맵 본문 전체는 데이터베이스에서 자동으로 꺼낼 수 없습니다.** 맵 내용은 별도 파일로 저장되고 DB에는 파일 위치만 기록되기 때문입니다.

다만 **다음은 가능합니다.**

- **직원이 추가·수정한 가지의 제목**은 작업 기록에 남아 조회 가능
- 협업(프로젝트)별 **개설자, 참여 인원, 가지 개수, 최종 수정 시각**은 조회 가능. `MEMBER_NAME`은 담당자가 아니라 협업방 등록 계정임을 Phase 0에서 확인함
- **누가 언제 무엇을 작업했는지에 대한 활동 기록**은 40만 건 이상 축적되어 있고 지금도 실시간으로 쌓이는 중

즉 "맵 안에 적힌 내용을 그대로 옮겨오는" 방식 대신, **"어떤 일이 어디까지 진행됐고 무엇이 멈춰 있는지"를 보여주는 방식**으로 설계했습니다. 실무적으로는 후자가 경영 판단에 더 직접적으로 쓰입니다.

**확인 결과, 가지 단위의 작업 내용은 조회할 수 있습니다.** 활동 기록(`work_log`)에 작업 구분과 함께 해당 가지의 제목이 남습니다. 최근 30일 기준 `추가` 4,346건 중 내용이 빈 것은 9건뿐이었습니다. 따라서 **"오늘 누가 어떤 내용을 추가·수정했는지"를 화면에 표시할 수 있습니다.** 맵 전체를 옮겨오지는 못하지만, 실질적으로 필요한 정보는 확보됩니다.

기록되는 작업 구분은 다음 8종입니다 (2020년 10월부터 누적 40만 건).

`ADD`(추가) · `EDIT`(수정) · `DEL`(삭제) · `MOVE`(이동) · `PASTE`(붙여넣기) · `LINK`(파일 연결) · `Link_Open`(파일 열람) · `Col_Print`(인쇄)

### 2-1. 진행 상태는 어떻게 판정하는가

**씽크와이즈에는 프로젝트의 진행 단계를 기록하는 기능이 없습니다.** 마인드맵 도구이지 공정 관리 도구가 아니기 때문입니다. 데이터베이스에 `COL_STAT`, `COMPLETE` 같은 상태 컬럼이 있기는 하나, 실제 값을 확인한 결과 **87건 전부 동일한 값**이었습니다 (`COL_STAT=1`, `COMPLETE='Y'`, `approve_stat=1`). 씽크와이즈가 협업방을 만들 때 넣는 기본값일 뿐, 업무 진행 상태와는 무관합니다.

따라서 **마지막으로 맵을 수정한 시각을 기준으로 자동 분류**합니다. 이 값은 사람이 입력하는 것이 아니라 저장할 때마다 시스템이 자동으로 남기므로, 별도 입력 없이 **일관된 기준으로 자동 반영됩니다.**

| 표시 | 기준 | 의미 |
|---|---|---|
| 활발 | 7일 이내 수정 | 지금 돌아가는 일 |
| 둔화 | 8~13일 | 속도가 떨어진 일 |
| 정체 | 14~29일 | 확인이 필요한 일 |
| 오래 멈춤 | 30일 이상 | 끝났거나 사실상 중단된 일 |

**자동 분류의 한계는 "끝난 일"과 "멈춘 일"을 구분하지 못한다는 점입니다.** 완료된 프로젝트도 한 달이 지나면 안 건드리기 때문입니다.

이를 보완하기 위해 **대표이사께서 화면에서 직접 `완료` / `진행` / `보류` 를 표시**하실 수 있게 합니다. 표시가 있으면 자동 분류보다 우선 적용됩니다.

- `완료`로 표시하면 마지막 수정일과 관계없이 **완료 상태로 고정**되고 활발·둔화·정체·오래 멈춤 집계에서 빠집니다.
- `진행` 또는 `보류`로 표시하면 해당 수동 상태로 고정됩니다.
- 수동 표시를 취소하면 즉시 마지막 수정일 기준의 자동 분류로 돌아갑니다. 예를 들어 최근 수정된 프로젝트의 `완료`를 취소하면 다시 `활발`이 됩니다.
- 자동 상태와 수동 상태는 API에서 함께 제공하되, 화면과 KPI에는 수동 표시가 반영된 **현재 상태**를 사용합니다.

이 표시는 **대시보드 자체 저장소에만 기록되며 씽크와이즈에는 어떤 영향도 주지 않습니다.** 30일 이상 멈춘 건은 기본적으로 접어두되, 전체 보기·상태 필터·검색으로 언제든 열어 완료 처리할 수 있게 합니다.

> 부수 효과: 이렇게 쌓인 완료·보류 이력은 씽크와이즈에 존재하지 않는 정보이며, 시간이 지날수록 회사의 자산이 됩니다.
### 2-2. 씽크와이즈 플래너 일정은 사용하지 않습니다

기획 초기에는 플래너의 일정을 화면에 올릴 계획이었으나, **실제 데이터를 확인한 결과 회사에서 쓰지 않는 기능이었습니다.**

- 삭제되지 않은 일정 129건 중 **가장 최근 것이 2026년 1월**
- 2023년 10월(54건) 이후 등록이 급감해 **최근 2년간 월 1~8건, 등록자는 거의 1명**
- 일정에 **시각이 없고 날짜만** 저장됨 (`2026-08-14` 형태) — 시간표 형태의 표시가 불가능

따라서 **일정 섹션을 빼고, 그 자리에 위의 "오늘 건드린 내용"을 넣었습니다.** 비어 있을 화면 대신 매일 갱신되는 화면이 들어가므로 손실이 아니라 교체입니다.


---

## 3. 화면 구성

개선 예시 화면: **`경영대시보드_예시화면_v2.html`** (브라우저로 열면 됩니다). 기존 `경영대시보드_예시화면.html`은 비교용 초안으로 남겨 둡니다.

예시 화면에 표시되는 이름과 숫자는 **설계 단계의 가상 데이터**입니다. 실제 수치는 구현 후 API가 운영 DB를 읽어오는 시점에 확정합니다.

### 상단 시각 요약

| 차트 | 내용 |
|---|---|
| 씽크와이즈 최근 활동 링 | 최근 15분 이내 활동 인원 / 최근 30일 사용자 비율 |
| 프로젝트 상태 도넛 | 활발·둔화·정체·오래 멈춤·완료의 현재 상태 분포 |
| 최근 안 읽은 메일 막대 | 3개 계정의 안 읽은 메일 합계와 계정별 비중 |

세 차트는 이미 `/api/dashboard`에 포함된 값만 사용하므로 새로운 집계 쿼리를 추가하지 않습니다. 회의에서 제외한 `전사 작업량 추이` 그래프는 다시 넣지 않습니다.

### 본문 섹션

| # | 섹션 | 보여주는 것 |
|---|---|---|
| 1 | **멈춰 있는 프로젝트** | 14~29일 정체된 협업을 경과일 긴 순으로. 개설자·참여 인원·최종 수정일 |
| 2 | **오늘 건드린 내용** | 직원들이 오늘 실제로 추가·수정한 가지의 제목 (최근순) |
| 3 | **최근 안 읽은 메일** | 다우오피스·Gmail·네이버 통합 — 제목·발신자·시각 표시, 5분 주기 갱신 (4장) |
| 4 | **프로젝트 현황판** | 현재 상태 분포 + 검색·상태 필터·전체 목록 + 대표이사 직접 표시·표시 취소 |
| 5 | **씽크와이즈 최근 활동** | 최근 15분 이내 활동한 사람과 마지막으로 활동한 프로젝트 |
| (보류) | **일정** | 씽크와이즈 플래너 / 구글 캘린더 중 선정 후 추가 (4-1장) |

### 회의에서 제외된 항목

대표이사 판단에 따라 아래 4개는 구현하지 않습니다.

| 제외 항목 | 사유 |
|---|---|
| 전사 작업량 추이 (14일 그래프) | 경영 판단에 쓰이지 않음 |
| 구성원별 가지 생성·수정 수 | 〃 |
| 오늘 오간 파일 | 〃 |
| 상단 KPI `오늘 작업 건수` | 〃 |

**개발 측면의 이점이 큽니다.** 제외된 4개가 모두 `work_log` 40만 행과 `link_updown`·`node_cnt_log`를 훑던 항목이라, **가장 무거운 집계가 전부 사라집니다.** 남은 조회도 캐시·증분 조회·주기 조절로 운영 DB 부담을 낮출 수 있고, 구축 기간도 짧아집니다.

### 표시 원칙

- 정체 기준일수(7/14/30일), 접속 판정 시간(15분) 등은 설정에서 변경 가능하게 만듭니다.
- 화면에는 **"이 분류가 어떻게 계산되는지"를 항상 함께 표시**합니다. 근거를 알 수 없는 숫자는 신뢰받지 못합니다.
- 긴 설명은 매일 보는 화면을 방해하지 않도록 `분류 기준 보기`에 접어 두고, 핵심 기준만 한 줄로 표시합니다.

### 3-1. 참고 — 설계 검증 당시 관측값 (2026-08-14)

아래 값은 설계 가능성을 확인하기 위해 DB에서 한 차례 관측한 값입니다. **예시 화면의 데이터는 가상이며, 운영 화면의 확정 수치는 매 갱신 시점에 API가 다시 계산합니다.**

| 항목 | 값 |
|---|---|
| 전체 협업 | 87건 |
| 활발(7일 이내) / 둔화 / 정체 / 오래 멈춤 | **25 / 4 / 5 / 53** |
| 등록 사용자 | 30명 |
| **최근 30일 내 접속한 사용자** | **7명** |

**주의할 점 둘.**

첫째, **`오래 멈춤 53건`** 은 2020년부터 6년간 쌓인 협업 전체가 대상이라 대부분 이미 끝난 건입니다. 화면에 처음 뜰 때 "회사가 방치 상태"로 오해되지 않도록, 시연 시 이 점을 먼저 설명하고 `완료` 표시로 정리하는 흐름을 보여드립니다.

둘째, 당시 **최근 30일 사용자는 7명**이었습니다. 등록은 30명이지만 최근 30일 내 접속 기록이 있는 사람은 7명이므로, `씽크와이즈 최근 활동` KPI는 `4명 / 최근 30일 사용자 7명`처럼 표시합니다. 분모와 분자는 운영 데이터에서 계속 다시 계산합니다.

---

## 4. 최근 안 읽은 메일 — 완전안으로 확정

회의 결과 **제목·발신자·시각까지 표시하는 완전안**으로 결정되었습니다.

- 다우오피스·Gmail·네이버 3개 계정의 **안 읽은 메일만** 한 목록으로 합쳐 최근순 표시
- 다우오피스 연동은 **실제 접속으로 검증 완료** (`imap.daouoffice.com:993`). 안 읽은 메일 개수가 웹메일 화면 숫자와 일치함
- Gmail·네이버는 앱 비밀번호 발급 후 동일한 방식으로 연결

**필요한 것**: 계정 3개의 접속 정보. Gmail·네이버는 앱 비밀번호를 발급하시면 되고, 다우오피스는 메일 비밀번호가 필요합니다. 자격증명은 서버 설정 파일에만 저장하며 화면이나 기록에는 남지 않습니다.

**대시보드는 안 읽은 메일의 제목·발신자·시각만 읽어오고 본문은 영구 저장하지 않습니다.** 또한 읽기 전용으로 접속하므로 대시보드가 조회했다는 이유로 메일이 '읽음' 처리되지 않습니다. 메일 카드에는 `5분 주기`와 마지막 갱신 시각을 별도로 표시합니다. 제목을 누르면 개별 메일이 아니라 해당 계정의 받은편지함을 엽니다.

## 4-1. 일정 섹션 — 검토 후 별도 제안 (보류)

일정을 씽크와이즈 플래너에서 가져올지, 구글 캘린더를 연결할지는 **담당자가 직접 사용해 본 뒤 더 나은 쪽을 제안**하기로 했습니다.

| | 씽크와이즈 플래너 | 구글 캘린더 |
|---|---|---|
| 현재 사용 상태 | **사실상 미사용** (2-2장) | 확인 필요 |
| 시각 정보 | 없음 (날짜만) | 있음 |
| 입력 편의 | PC의 씽크와이즈에서만 | 휴대폰·PC 어디서나 |
| 알림 | 없음 | 있음 |
| 연동 난이도 | 낮음 (같은 DB) | 낮음 (비공개 iCal 주소 사용 시) |
| 안정성 | 벤더 스키마 변경에 영향받음 | 표준 형식이라 안정적 |

**현시점 판단은 구글 캘린더가 우세합니다.** 씽크와이즈 플래너는 이미 죽어 있어 되살리려면 조직 전체의 입력 습관을 바꿔야 하는데, 그건 대시보드가 해결할 수 있는 문제가 아닙니다.

기술적으로도 구글 캘린더가 간단합니다. 구글 캘린더 설정의 **비공개 iCal 주소**를 쓰면 별도 인증 절차 없이 일정 파일을 주기적으로 읽어올 수 있습니다.

> **단, 어느 쪽을 고르든 전제 조건은 하나입니다 — 대표이사께서 일정을 실제로 그 도구에 입력하셔야 합니다.** 지금 수첩이나 머릿속으로 관리하고 계시다면 두 방법 모두 빈 화면이 됩니다. 제안 전에 **"지금 일정을 어디에 적고 계신지"** 를 먼저 확인할 것.

---

## 5. 결정 사항과 남은 항목

**2026-08-14 회의에서 결정된 것**

- 메일: **완전안** (제목·발신자·시각 표시)
- 제외: 전사 작업량 추이 / 구성원별 활동량 / 오늘 오간 파일 / KPI `오늘 작업 건수`
- 일정 섹션: **보류** — 담당자가 검토 후 별도 제안 (4-1장)
- 상태 표시: 수동 `완료`/`진행`/`보류`가 자동 분류보다 우선하며, 표시 취소 시 자동 분류로 복귀
- 갱신 주기: 씽크와이즈 1분 / 메일 5분
- 외부 접속: HTTPS + 허용된 사용자·기기만 접근. 별도 로그인 화면은 두지 않는 방향

**아직 필요한 것**

1. **메일 계정 3개의 접속 정보** — Gmail·네이버 앱 비밀번호 발급, 다우오피스 메일 비밀번호
2. **외부 접속에 사용할 대표이사 계정과 기기** — PC·휴대폰 등 Tailscale을 설치할 기기 목록
3. **대표이사께서 현재 일정을 어디에 적고 계신지** (4-1장 제안을 위해)
4. 정체 판정 기준일수 — 기본값 7 / 14 / 30일로 진행하고, 써 보신 뒤 조정

> 서버에 별도 개발 도구를 설치할 필요는 없습니다. Python 3.11.9가 이미 설치되어 있고, 상주화도 Windows 기본 기능인 작업 스케줄러로 처리합니다. 외부 비공개 HTTPS 접속을 위해서는 Tailscale 클라이언트만 추가 설치합니다.

---
---

# 개발 지시서 (이하 클로드 코드용)

> 이 지점 이후를 클로드 코드에 붙여넣습니다.

## 6. 실행 환경

| 항목 | 값 |
|---|---|
| 배포 위치 | 사내 서버 PC (호스트명 `DESKTOP-318VJ68`, `192.168.0.76`) — DB와 같은 장비 |
| 접근 방식 | Tailscale Serve가 제공하는 비공개 HTTPS 주소. 사내·외부에서 동일한 주소 사용 |
| OS | Windows · **Python 3.11.9 기설치 확인** (별도 설치 불필요) |
| 포트 | 8080 (사용 중이 아님을 확인) |
| 설치 경로 | `D:\YJDashboard` (씽크와이즈 폴더 밖). D 드라이브 여유 6.2TB |
| DB | MariaDB 10.2, 같은 장비의 `localhost:3306` |
| DB 계정 | 읽기 전용 (`SELECT`만). 원격 개발 시에는 `tw_ro@192.168.0.%` |
| 문자셋 | `utf8` (utf8mb4 아님 — 커넥션 charset을 `utf8`로 고정할 것) |

애플리케이션은 `127.0.0.1:8080`에만 바인딩하고, 외부 HTTPS 종단은 **Tailscale Serve**가 맡는다. 대표이사와 지정 유지보수 담당자의 계정·기기만 포트에 접근할 수 있도록 Tailscale Grants와 애플리케이션 허용 목록을 설정한다. 공유기 포트포워딩과 공개 인터넷 노출은 하지 않는다. 이 방식이면 앱 자체 로그인 화면 없이도 허용된 기기에서 주소를 여는 것만으로 접속할 수 있다.

> HTTPS 인증서 발급 전 서버의 Tailscale 장치 이름을 `yj-dashboard`처럼 민감정보가 없는 이름으로 변경한다. 인증서의 장치 이름과 tailnet DNS 이름은 Certificate Transparency 공개 기록에 남을 수 있기 때문이다.

**절대 금지**
- `INSERT` / `UPDATE` / `DELETE` / DDL — 운영 중인 상용 패키지(씽크와이즈)의 DB임
- `D:\ThinkWise` 데이터 파일 직접 읽기
- `collaboration_user.PASSWORD` 컬럼 조회 (비밀번호가 평문 또는 약한 해시로 저장되어 있음)
- DB 비밀번호·메일 자격증명을 소스 코드나 OneDrive 동기화 폴더에 저장 — 반드시 `.env` + `.gitignore`
- Tailscale Funnel 사용 또는 공유기 포트포워딩 — 대시보드를 공개 인터넷에 노출하면 안 됨

## 7. 기술 스택

- **백엔드**: Python 3.11+ / FastAPI / uvicorn / PyMySQL
- **프론트**: 단일 HTML + 바닐라 JS (빌드 도구 없음). 승인된 개선 목업 `경영대시보드_예시화면_v2.html`을 기준으로 더미 값만 API 데이터로 치환
- **상주화**: **Windows 작업 스케줄러** — 트리거 `컴퓨터 시작 시`, `사용자 로그온 여부에 관계없이 실행`, `가장 높은 권한으로 실행`, `실패 시 재시작`. 프로세스 관리용 외부 도구가 필요 없으므로 NSSM보다 우선한다. 운영 중 불안정하면 그때 NSSM으로 전환
- **가상환경 필수** — 서버의 시스템 Python을 오염시키지 않도록 `D:\YJDashboard\.venv` 사용
- **의존성 최소화 원칙** — 사내 서버에 무거운 런타임을 새로 깔지 않는다

## 8. 성능 설계 (중요)

**회의에서 4개 섹션이 빠지면서 무거운 집계가 전부 사라졌습니다.** 14일 추이(`work_log` 40만 행 전량 스캔), 구성원별 활동량(`node_cnt_log`), 오늘 오간 파일(`link_updown`) 이 모두 제외되어 남은 것은 **오늘치 조회와 협업 87건 분류뿐**입니다.

그래도 캐시 구조는 유지합니다. 브라우저가 열릴 때마다 DB를 때리는 구조는 탭이 늘어나는 만큼 부하가 곱해지고, 메일(IMAP)은 응답이 느려 화면을 붙잡기 때문입니다.

**반드시 서버 측 캐시 구조로 만들 것.**

```
씽크와이즈 워커 ──1분마다 1회──> DB 조회 ──> 메모리에 JSON 캐시
메일 워커 ──5분마다 1회──> IMAP 조회 ──> 별도 메모리 캐시
브라우저 ──1분마다──> GET /api/dashboard ──> 캐시된 JSON 즉시 반환
```

- 브라우저 수와 무관하게 DB 부하는 1분당 1회로 고정
- 워커 쿼리 실패 시 직전 캐시를 유지하고 화면에 "갱신 실패, N분 전 데이터" 표시
- MariaDB 동시 연결은 최대 2개로 제한한다. PyMySQL 자체에는 풀 기능이 없으므로 워커별 연결을 재사용하고, 끊어졌을 때만 재연결한다

**인덱스 실측 결과 (2026-08-14 확인)**

`tw_colla_log` 에 존재하는 인덱스는 `join_exit.PRIMARY`, `link_updown.PRIMARY`, `work_log.PRIMARY` **3개뿐**입니다. `conn_usertime`, `login_log`, `node_cnt_log`, `install_log`, `mail_log` 는 인덱스가 전혀 없습니다.

즉 **날짜 조건 조회는 전부 풀스캔**입니다. `c_date >= CURDATE()` 조건도 날짜 인덱스가 없으므로 스캔 범위를 줄이지는 못합니다. 다만 실제 데이터 크기가 아래와 같아 현재 실측상 대부분은 문제가 되지 않습니다.

| 테이블 | 크기 | 판단 |
|---|---|---|
| work_log | 73MB | **유일하게 주의 필요** |
| join_exit / link_updown | 각 5.5MB | 무시 가능 |
| login_log / node_cnt_log | 각 1.5MB | 무시 가능 |
| conn_usertime | 96KB | 무시 가능 |

**인덱스를 추가하지 말 것** — 읽기 전용 계정이며 상용 패키지의 스키마입니다. 대응은 다음과 같습니다.

- `work_log`는 첫 조회 시 오늘 데이터를 읽고, 이후에는 확인된 기본키를 커서로 사용해 새 행만 증분 조회한다. 구현 전 기본키가 시간 순으로 증가하는지 확인한다
- 기본키 증분 조회를 신뢰할 수 없으면 `work_log`만 5분 주기 풀스캔으로 낮춘다. 날짜 조건만으로 스캔이 줄어든다고 가정하지 않는다
- 메일(IMAP)은 **DB 워커와 분리된 5분 주기**로 돌린다. 느린 IMAP이 1분 주기의 씽크와이즈 갱신을 붙잡으면 전체 화면이 늦어진다

**성능 실측 결과 (원격 접속 기준)**

| 쿼리 | 소요 |
|---|---|
| 14일 일별 집계 (`work_log` 40만 행) — *현재는 미사용* | 0.098초 |
| 구분별 전체 집계 (6년치 전량) — *현재는 미사용* | 0.448초 |
| 협업 87건 분류 | **0.001초** |

가장 무거웠던 쿼리도 당시 약 0.1초였고 `innodb_buffer_pool_size`가 2019MB라 73MB 테이블이 메모리에 상주합니다. 다만 운영 DB의 여유를 우선해 **씽크와이즈 기본 갱신 주기는 1분**으로 정하고, `work_log`는 위의 증분 조회 또는 5분 폴백을 사용합니다.

## 9. 데이터 정의

### 9-1. 조회 가능한 테이블 (실측)

| 스키마 | 테이블 | 행 수 | 용도 |
|---|---|---|---|
| tw_colla_log | work_log | 406,216 | 작업 기록 (`gubun` 구분, `detail` 상세, `c_date`) |
| tw_colla_log | join_exit | 40,837 | 협업 참여/종료 — 현재 미사용 |
| tw_colla_log | link_updown | 30,131 | 파일 업/다운 — **미사용 (회의에서 제외)** |
| tw_colla_log | node_cnt_log | 7,528 | 가지 생성/수정 수 — **미사용 (회의에서 제외)** |
| tw_colla_log | login_log | 16,429 | 로그인 — 실사용자 판정에만 사용 |
| tw_colla_log | conn_usertime | 778 | 사용자별 최종 접속 시각 (`uptime`) |
| tw_colman | collaboration_board | 87 | 협업(프로젝트) 마스터 |
| tw_colman | collaboration_participant | 393 | 협업 참여자 |
| tw_colman | collaboration_user | 30 | 사용자 (`DEPART`, `GRADE`, `MEMBER_NAME`) |
| tw_colman_planner | planner_schedule | 211 | 일정 — **미사용, 쓰지 않음** (2-2장) |
| tw_colman_planner | planner_option | 182 | 플래너 항목 |

빈 테이블(사용 안 함): `fd_board`, `col_mail`, `mail_log`, `collabo_notice`, `install_log`, `tw_category`, `statistics_weights`, `notice_category`, `core_collabo`, `collabo_sub_moder`

### 9-2. 섹션별 쿼리 (초안 — 실행 전 검증 필요)

조인 키는 협업 인덱스 `hashfname` = `collaboration_board.HASHFNAME` 입니다.

```sql
-- [A] 지금 접속 중
-- 같은 사용자의 기록이 여러 건이면 최신 1건만 화면에 표시한다.
-- MariaDB 10.2의 ROW_NUMBER 사용 가능 여부를 실제 서버에서 다시 확인할 것.
SELECT x.u_id, u.MEMBER_NAME, x.uptime, b.TITLE
FROM (
  SELECT c.*,
         ROW_NUMBER() OVER (PARTITION BY c.u_id ORDER BY c.uptime DESC) AS rn
  FROM tw_colla_log.conn_usertime c
  WHERE c.uptime >= NOW() - INTERVAL 15 MINUTE
) x
LEFT JOIN tw_colman.collaboration_user u ON u.MEMBER_ID = x.u_id
LEFT JOIN tw_colman.collaboration_board b ON b.HASHFNAME = x.hashfname
WHERE x.rn = 1
ORDER BY x.uptime DESC;

-- [B] 최근 30일 사용자 수 — KPI 분모
SELECT COUNT(DISTINCT u_id) AS active_users
FROM tw_colla_log.conn_usertime
WHERE uptime >= NOW() - INTERVAL 30 DAY;

-- [D] 프로젝트 현황 + 활동 기반 자동 분류
--     ※ COL_STAT / COMPLETE / approve_stat 는 87건 전부 동일값(1 / 'Y' / 1)이므로 사용하지 않는다.
--     ※ UPD_DATE 가 NULL 일 수 있으므로 COALESCE 로 폴백한다.
SELECT b.SEQ, b.HASHFNAME, b.TITLE, b.MEMBER_NAME, b.TREE_CNT,
       COALESCE(b.UPD_DATE, b.REG_DATE, b.CRT_DATE) AS last_touch,
       DATEDIFF(NOW(), COALESCE(b.UPD_DATE, b.REG_DATE, b.CRT_DATE)) AS idle_days,
       (SELECT COUNT(*) FROM tw_colman.collaboration_participant p
         WHERE p.COL_IDX = b.SEQ) AS member_cnt
FROM tw_colman.collaboration_board b
WHERE COALESCE(b.DEL_YN,'N') <> 'Y'
ORDER BY last_touch DESC;

-- 자동 분류는 애플리케이션에서 idle_days 로 계산한다 (임계값을 설정에서 바꿀 수 있도록):
--   idle_days <=  7  → active   (활발)
--   idle_days <= 13  → slowing  (둔화)
--   idle_days <= 29  → idle     (정체)   ← "멈춰 있는 프로젝트" 섹션의 대상
--   idle_days >= 30  → dormant  (오래 멈춤)
-- 9-4의 수동 표시가 있으면 current_status는 수동 표시가 되고,
-- 표시를 취소하면 current_status는 다시 auto_status가 된다.

-- [G] 오늘 건드린 내용  (기존 '오늘 일정' 을 대체)
--     gubun 은 8종: ADD / EDIT / DEL / MOVE / PASTE / LINK / Link_Open / Col_Print
--     화면에는 ADD·EDIT·DEL·MOVE·PASTE·LINK 를 노출하고 Link_Open·Col_Print 는 제외한다(열람·인쇄는 내용 변경이 아님)
SELECT w.c_date, w.u_name, w.gubun, w.detail, b.TITLE AS project
FROM tw_colla_log.work_log w
LEFT JOIN tw_colman.collaboration_board b ON b.HASHFNAME = w.hashfname
WHERE w.c_date >= CURDATE()
  AND w.gubun IN ('ADD','EDIT','DEL','MOVE','PASTE','LINK')
  AND w.detail IS NOT NULL AND w.detail <> ''
ORDER BY w.c_date DESC
LIMIT 30;
```

위 `[G]` 쿼리는 최초 로딩·폴백용이다. 날짜 인덱스가 없으므로 반복 실행 시 풀스캔이 발생한다. 정상 운영에서는 `work_log`의 기본키를 확인한 뒤 마지막으로 읽은 키 이후의 새 행만 가져와 메모리 목록에 합친다.

> ※ `planner_schedule` 을 쓰는 '오늘 일정' 쿼리는 2-2장의 사유로 삭제되었다.

### 9-3. 구현 착수 전 확인할 것 (Phase 0)

기존 데이터 구조 확인은 완료했으며, 3판 기술 검토에서 운영 정확도를 위해 아래 4가지를 구현 직전에 추가 확인하기로 했다. 전체 구조를 바꾸는 항목은 아니지만 결과를 확인하기 전에는 관련 명칭과 쿼리를 확정하지 않는다.

| 항목 | 결과 |
|---|---|
| `collaboration_board` 상태 컬럼 | 87건 전부 동일값 → **사용 불가**. 활동 기반 분류로 대체 (2-1장) |
| `work_log.gubun` | 8종 확정 (`ADD`/`EDIT`/`DEL`/`MOVE`/`PASTE`/`LINK`/`Link_Open`/`Col_Print`) |
| `work_log.detail` | **가지 제목이 기록됨.** 빈 값 비율 0.2% → "오늘 건드린 내용" 섹션 확정 |
| `planner_schedule` | **사실상 미사용.** 날짜만 저장(시각 없음), 최신 2026-01 → 섹션 제외 (2-2장) |
| `collaboration_participant.COL_IDX` ↔ `board.SEQ` | 404행 중 404행 매칭 → **참여 인원수 신뢰 가능** |
| `collaboration_user` | 등록 30명 / 최근 30일 접속 **7명** / `DEPART` 전원 채워짐(15개 부서) |
| 인덱스 | 8장에 반영 |
| 성능 | 8장에 반영 |
| 다우오피스 IMAP | **접속 성공 확인** (12장) |
| `collaboration_board.UPD_DATE` 의미 | `work_log` 최신 시각과 표본 대조해 실제 맵 수정 시각인지 확인 필요 |
| `collaboration_board.MEMBER_NAME` 의미 | 87건 모두 `MEMBER_ID=REG_ID`, 참여자 일치는 2건 → 업무 담당자가 아닌 **협업방 개설자**로 확정 |
| `conn_usertime` 중복 | 사용자별 최신 1건만 남기는 쿼리 결과 검증 필요 |
| `work_log` 기본키 | 시간 순 증가 여부 확인 후 증분 커서로 사용. 불가하면 5분 풀스캔 |

**부서 단위 집계는 권장하지 않는다.** 30명에 15개 부서라 부서당 평균 2명이고, 실사용자 7명 기준이면 대부분의 부서가 0으로 표시된다.

**남은 선택 항목 (구현을 막지 않음)**

`tw_colman_planner.planner_option` 의 `OptionType='Projects'`, `KindNum='1001'` 이 42건 / 7명 / 오늘 갱신 중으로 살아 있다. 이 7명은 실사용자 7명과 일치한다. `Subject`, `DataAll`(평균 315자)에 프로젝트 분류 정보가 들어 있다면 부가 섹션을 만들 수 있으나, **없어도 8개 섹션 구성에는 영향이 없다.** Phase 2 이후 여유가 있을 때 검토한다.

### 9-4. 대시보드 자체 저장소 (SQLite)

씽크와이즈 DB는 읽기 전용이므로, 대시보드가 만들어내는 정보는 **별도 SQLite 파일**에 저장한다. 위치는 대시보드 설치 폴더 하위(`data/dashboard.db`), 씽크와이즈 폴더 밖.

```sql
-- 대표이사가 직접 지정한 프로젝트 상태 (자동 분류보다 우선)
CREATE TABLE project_mark (
  hashfname   TEXT PRIMARY KEY,   -- collaboration_board.HASHFNAME
  mark        TEXT NOT NULL,      -- 'done' | 'running' | 'hold'
  memo        TEXT,
  updated_at  TEXT NOT NULL
);

-- 설정값 (정체 기준일수, 접속 판정 시간 등)
CREATE TABLE setting (
  k TEXT PRIMARY KEY,
  v TEXT NOT NULL,
  updated_at TEXT NOT NULL  -- 설정 변경 감사 시각(KST ISO 8601)
);
```

`project_mark`는 씽크와이즈에 없는 정보이므로 **백업 대상**이다. 하루 1회 SQLite 백업 API로 `data/backups`에 일관된 사본을 만들고 최근 30일분을 보관한다. 구축 완료 전 복원 시험을 1회 수행한다.

API:

```
PUT    /api/project/{hashfname}/mark   { "mark": "done", "memo": "" }
DELETE /api/project/{hashfname}/mark   수동 표시 취소 → 자동 분류로 복귀
```

이 엔드포인트만 쓰기 권한을 가지며, 대상은 SQLite뿐이다. **MariaDB에는 어떤 경로로도 쓰기가 발생하지 않는다.**

## 10. API 설계

```
GET  /                    대시보드 HTML
GET  /api/dashboard       캐시된 전체 데이터 (JSON) — 프론트가 1분마다 폴링
GET  /api/health          워커 상태, 마지막 갱신 시각, 마지막 오류
GET  /api/settings        정체 기준일수·접속 판정 시간 조회
PUT  /api/settings        설정 변경
PUT  /api/project/{hashfname}/mark    완료·진행·보류 표시
DELETE /api/project/{hashfname}/mark  수동 표시 취소
```

별도 로그인·로그아웃 API는 두지 않는다. Tailscale Serve가 전달한 사용자 신원과 Grants가 접근을 통제하며, 애플리케이션은 쉼표로 구분한 허용 목록에 등록된 대표이사·유지보수 계정의 요청만 받는다.

`/api/dashboard` 응답 구조

```json
{
  "generated_at": "2026-08-14T10:31:02+09:00",
  "stale": false,
  "kpi": { "online_now": 4, "active_users_30d": 7,
           "active": 25, "slowing": 4, "idle": 5, "dormant": 53,
           "running": 0, "done": 0, "hold": 0, "total_projects": 87 },
  "idle_projects":  [ { "hashfname": "...", "title": "...", "creator": "...", "members": 4,
                        "last_touch": "2026-07-19", "idle_days": 26 } ],
  "projects":       [ { "hashfname": "...", "title": "...", "creator": "...",
                        "auto_status": "active", "mark": null, "current_status": "active",
                        "members": 6, "tree_cnt": 418, "last_touch": "...", "idle_days": 0 } ],
  "recent_edits":   [ { "at": "11:19", "who": "...", "gubun": "ADD",
                        "detail": "...", "project": "..." } ],
  "online_users":   [ { "name": "...", "project": "...", "at": "11:19" } ],
  "mail":           { "refresh_interval_sec": 300,
                       "fetched_at": "2026-08-14T11:30:00+09:00",
                      "unread_total": 20,
                      "items": [ { "account": "daou", "from": "...", "subject": "...",
                                   "at": "10:22", "url": "..." } ] }
}
```

`activity_14d`, `member_activity`, `files_today`, `kpi.today_work` 는 회의 결정에 따라 **응답에 포함하지 않는다.** 해당 쿼리도 만들지 않는다.

`mail`은 별도 5분 주기로 갱신되므로 `fetched_at`을 따로 둔다. `items`에는 `UNSEEN` 결과만 넣는다. IMAP 조회가 실패해도 나머지 화면은 정상 표시되어야 하며, 메일 카드에만 "메일 갱신 실패 · N분 전 데이터"를 표시한다.

`idle_projects`에는 `current_status == "idle"`인 프로젝트만 넣는다. 자동으로 정체였더라도 수동 `완료`·`진행`·`보류` 상태이면 정체 목록과 자동 상태 KPI에서 빠진다.

## 11. 보안 요구사항

- 대표이사 전용 화면이며 **직원 이름·활동 시각과 메일 제목이 포함**되므로 공개 인터넷에 직접 노출하면 안 됨
- 외부 접속은 **Tailscale Serve + HTTPS**를 권장안으로 사용한다. Serve는 tailnet 안의 기기에만 로컬 서비스를 공유하고 HTTPS를 제공한다
- Tailscale Grants는 대표이사·지정 유지보수 계정의 허용 기기에서 대시보드 서버의 HTTPS 포트로 가는 연결만 허용한다
- FastAPI는 `127.0.0.1:8080`에만 바인딩한다. Tailscale Serve를 우회해 LAN이나 인터넷에서 직접 접근할 수 없어야 한다
- 앱 자체 로그인 화면은 두지 않는다. Tailscale Serve의 `Tailscale-User-Login` 헤더가 설정된 복수 허용 계정 중 하나와 일치하는지 확인해 투명하게 인증한다
- **Tailscale Funnel과 공유기 포트포워딩을 사용하지 말 것.** 둘 중 하나라도 사용하면 로그인 없는 화면이 공개될 수 있다
- Tailscale을 도입하지 않는 경우에는 외부 접속을 열지 않는다. 다른 공개 HTTPS 방식을 선택한다면 앱 로그인 또는 동등한 인증이 필수다
- 비밀번호·자격증명은 전부 `.env`. 저장소·OneDrive 동기화 폴더에 올리지 않음
- `.env`와 SQLite 파일은 서비스 실행 계정과 관리자만 읽을 수 있도록 Windows ACL을 제한
- 로그에 개인정보(메일 주소, 메일 제목, IP)를 남기지 않음
- 모든 HTML·API 응답에 `Cache-Control: no-store` 적용. API는 CORS를 열지 않고 상태 변경 요청의 `Origin`을 확인
- DB의 가지 제목과 메일 헤더는 신뢰할 수 없는 입력으로 취급하고, 프론트에서는 `innerHTML`이 아니라 `textContent`로 출력

참고: [Tailscale Serve 공식 문서](https://tailscale.com/docs/features/tailscale-serve) · [HTTPS 설정](https://tailscale.com/docs/how-to/set-up-https-certificates) · [Grants 접근 제어](https://tailscale.com/docs/features/access-control/grants)

## 12. 메일 연동 (완전안 — 확정)

3개 계정(다우오피스·Gmail·네이버)의 **최근 안 읽은 메일**을 한 목록으로 합쳐 제목·발신자·시각을 표시한다.

다우오피스 접속 파라미터 (2026-08-14 실제 접속 확인)

```
서버   : imap.daouoffice.com
포트   : 993 (SSL/TLS)
아이디 : 전체 메일 주소 (예: yec20@yec95.com)
```

- **아이디는 반드시 전체 메일 주소.** `imap.daouoffice.com` 은 여러 테넌트가 공유하는 진입점이라, 아이디만 보내면 서버가 인증 응답 없이 연결을 끊는다(`socket error: EOF`). 이는 인증 실패가 아니므로 오류 처리 시 구분할 것
- 테넌트 서브도메인(`imap.yec.daouoffice.com`)은 **DNS에 존재하지 않는다.** 공용 진입점을 써야 한다
- 관리자 설정(App 관리 > 메일 > 메일 그룹)에서 `POP` / `IMAP` / `SMTP AUTH` 가 모두 허용되어 있음을 확인함. 개인별로 다시 끌 수 있는 `멤버 메일 개별 설정` 이 존재하므로, 대표이사 계정도 켜져 있는지 확인 후 진행
- Gmail: 앱 비밀번호 발급 → `imap.gmail.com:993`
- 네이버: 메일 환경설정에서 IMAP 사용 설정 + 앱 비밀번호 → `imap.naver.com:993`

**구현 규칙**

- Python `imaplib` 사용. `UNSEEN` 검색 결과 중 최근 N건의 **헤더(발신자·제목·날짜)만** `BODY.PEEK[HEADER.FIELDS ...]`로 조회. **본문은 가져오지 않는다**
- `INBOX` 는 반드시 `readonly=True` 로 선택한다. 그러지 않으면 대시보드가 읽는 것만으로 메일이 '읽음' 처리되어 **대표이사의 안 읽은 메일 표시가 사라진다**
- 메일 조회는 대시보드 워커와 **분리된 주기(5분)** 로 실행. IMAP은 느리고 계정 잠금 위험이 있다
- **로그인 실패 시 자동 재시도 금지.** 실패를 기록하고 다음 주기까지 대기한다. 연속 시도는 계정 잠금을 유발한다
- 자격증명은 `.env` 에만 저장하고 로그에 남기지 않는다
- 세 계정의 날짜를 한국 시간으로 정규화한 뒤 합쳐 최근순으로 정렬한다. 날짜 해석 실패 항목은 목록 끝에 두고 오류를 기록하되 제목·주소는 로그에 남기지 않는다
- 제목 클릭은 해당 공급자의 **받은편지함 주소**를 연다. IMAP 정보만으로 특정 메시지의 웹메일 주소를 만들지 않는다

## 13. 구현 순서

| Phase | 내용 | 산출물 |
|---|---|---|
| ~~0~~ | ~~코드값·인덱스·성능·IMAP 확인~~ | **완료 (2026-08-14)** |
| 0-1 | 3판 기술 검토 후속 확인 4건 (9-3장) | 필드 의미·중복·증분키 검증 결과 |
| 1 | FastAPI 골격 + DB 캐시 워커 + `/api/dashboard` | 실데이터 JSON |
| 2 | 예시 화면을 실데이터에 연결 (메일 제외) | 동작하는 대시보드 |
| 3 | SQLite 저장소 + 대표이사 직접 표시·취소 기능 + 설정 화면 | 자동 분류 보정 가능 |
| 4 | 메일 섹션 (완전안, 3개 계정) | |
| 5 | 작업 스케줄러 등록 + Tailscale Serve·Grants·HTTPS + 외부 접속 검증 | 안전한 상시 가동 |
| (추후) | 일정 섹션 — 4-1장 결론에 따라 | |

각 Phase 완료 시 허용된 다른 기기에서 실제로 열리는지 확인한 뒤 다음으로 넘어간다. Phase 5에서는 휴대폰의 Wi-Fi를 끈 외부망에서도 HTTPS 접속되는지, 허용하지 않은 기기에서는 접속되지 않는지를 모두 확인한다.

**예상 소요** — 섹션 4개가 빠져 초판보다 짧아졌다.

| Phase | 기간 |
|---|---|
| 1 · 백엔드 + 캐시 워커 | 1.5일 |
| 2 · 화면 연결 | 1.5일 |
| 3 · SQLite + 직접 표시·설정 | 1일 |
| 4 · 메일 3개 계정 | 2일 |
| 5 · 상주화 + 비공개 HTTPS 외부 접속 | 1일 |
| **합계** | **약 7 근무일** |

메일이 남은 항목 중 가장 큰 변수다. 다우오피스는 검증됐으나 Gmail·네이버 앱 비밀번호 발급이 대표이사 쪽 작업이라, 그 대기 시간에 따라 Phase 4가 밀릴 수 있다. **Phase 4를 마지막에 두고 1~3·5를 먼저 끝내 두면 대기 시간이 일정에 영향을 주지 않는다.**

## 14. 디자인 규격 — 개선안

개선 목업 `경영대시보드_예시화면_v2.html`을 검토 기준으로 사용한다. 기존 화면의 문제는 모든 카드의 강조도가 비슷하고, 작은 글자와 긴 설명·넓은 표가 동시에 보여 시선이 분산된다는 점이다.

- 첫 화면의 순서를 **요약 → 지금 확인할 프로젝트 → 최근 변경·안 읽은 메일 → 전체 프로젝트**로 고정한다
- 상단에는 `씽크와이즈 최근 활동` 링, `프로젝트 현재 상태` 도넛, `안 읽은 메일 계정별 비중` 막대를 배치해 숫자 카드의 반복을 줄인다
- 정체 프로젝트를 가장 먼저 행동할 영역으로 강조하고, 각 행에서 `완료`·`진행`·`보류`를 바로 지정할 수 있게 한다
- 프로젝트 현황은 **큰 도넛 차트 + 직접 표시된 범례**로 전체 분포를 보여주고, 상세 영역에서는 상태별 필터·전체 보기·검색을 제공한다
- 전체 프로젝트 표는 프로젝트명·현재 상태·개설자·최종 수정·상태 변경만 우선 표시한다. 참여 인원과 가지 수는 상세 정보로 내린다
- 긴 설명과 DB 테이블명은 본문에서 제거하고 `분류 기준 보기`에 접어 둔다
- 활동 분류 색: 활발 초록 / 둔화 노랑 / 정체 주황 / 오래 멈춤 회색 / 완료 파랑 / 보류 보라. 모든 색에는 텍스트 라벨을 동반한다
- 메일 계정은 색과 `다우`·`G`·`N` 텍스트를 함께 사용한다
- 상태 변경과 필터는 실제 `<button>`·`<select>` 요소를 사용하고 키보드 포커스를 유지한다
- 1100px 이하에서는 한 열로 재배치하되, `오늘 건드린 내용`과 `최근 안 읽은 메일`이 전체 프로젝트 표보다 먼저 오도록 한다
- 표는 좁은 화면에서 카드형 행으로 바꾸거나 표 영역 안에서만 가로 스크롤한다
- 로딩·빈 데이터·전체 갱신 실패·메일만 갱신 실패의 네 가지 상태를 별도로 설계한다
- 라이트/다크 모드를 지원하고 숫자는 `font-variant-numeric: tabular-nums`로 정렬한다

## 15. 개정 3판 기술 검토 결과

**검토 결론: 구현 가능한 구조이며, 즉시 구조를 다시 설계해야 할 차단 요소는 없다.** 다만 아래 항목을 구현 착수 전 체크리스트로 고정한다.

1. `UPD_DATE`는 실제 활동 시각과 일치하지 않아 공유 `work_log` 색인으로 대체했고, `MEMBER_NAME`은 등록 계정 관계를 전수 집계해 화면 명칭을 `개설자`로 확정한다.
2. `conn_usertime`은 사용자별 최신 행만 남기는지 검증하고, 최근 30일 사용자 수를 API에서 동적으로 계산한다.
3. `work_log` 기본키가 증분 커서로 안전한지 확인한다. 불가능하면 이 조회만 5분 풀스캔으로 운영한다.
   `PASTE`는 실제 내용 변경이므로 오늘 건드린 내용에 포함하고, `Link_Open`·`Col_Print`만 제외한다.
4. uvicorn은 **단일 프로세스**로 실행한다. 여러 worker를 띄우면 DB·IMAP 백그라운드 워커가 중복 실행된다.
5. 수동 상태는 `auto_status`와 `current_status`를 분리하고, KPI·필터·목록은 `current_status`를 사용한다. 표시 취소 시 SQLite 행을 삭제하고 자동 상태로 돌아간다.
6. Tailscale Serve를 사용할 때 FastAPI는 localhost에만 바인딩하고, 등록된 대표이사·유지보수 허용 목록의 신원 헤더를 확인한다. Funnel은 비활성화한다.
7. SQLite 일일 백업은 같은 파일을 단순 복사하지 말고 SQLite 백업 API를 사용하며, 최근 30일 보관 및 복원 시험 절차를 둔다.
8. 상태 변경·설정 API는 허용 값 검증, 동일 출처 확인, 감사 시각 기록을 적용한다.

외부 접속의 UX와 보안을 동시에 만족하려면 **대표이사와 지정 유지보수 기기에 Tailscale을 한 번 설치·로그인하고, 이후에는 별도 대시보드 로그인 없이 HTTPS 주소를 여는 방식**이 가장 단순하다. 임의의 PC에서 주소만 입력해 접속하는 공개 방식이 필요해지면 그때는 별도 로그인을 다시 도입해야 한다.

## 16. 개발 진행 자동 기록

개발 진행의 정본은 **`경영대시보드_개발진행현황.md`**로 한다. Codex·Claude 등 저장소에서 작업하는 개발 도구는 소스·설정·테스트·문서를 변경한 같은 작업 안에서 이 문서를 반드시 갱신한다.

- 상단에 마지막 갱신 시각(KST), 현재 단계, 완료 Phase 수, 다음 작업, 차단 사항을 유지한다
- Phase별 `대기` / `진행 중` / `검증 중` / `완료` 상태와 실제 완료 조건을 표시한다
- 모든 논리적 변경을 변경 기록에 순서대로 추가한다
- 변경 목적, 영향 파일, 실행한 검증과 결과를 기록한다
- 실패·미검증·차단을 숨기지 않으며 비밀값은 기록하지 않는다
- 파일 변경이 없는 읽기 전용 조사만 기록 대상에서 제외한다

이 규칙은 `AGENTS.md`와 `CLAUDE.md`에도 저장소 필수 지침으로 둔다. 따라서 이후 구현 작업은 진행 문서 갱신까지 끝나야 한 작업이 완료된 것으로 본다.

---

## 부록 · 알려진 보안 이슈 (대시보드와 별개, 기록용)

씽크와이즈는 상용 패키지이므로 **임의로 수정하지 말고 벤더 지원 범위를 확인한 뒤** 진행할 것.

1. `D:\ThinkWise\WebService\BasicClass\Config.asp` 에 DB `root` 비밀번호가 평문 저장, 웹서비스가 root 권한으로 DB 접속 중
2. `collaboration_user.PASSWORD` 가 `varchar(50)` — 사용자 비밀번호가 평문 또는 약한 해시
3. 조회 전용 계정 `tw_ro` 의 비밀번호가 숫자 7자리로 약함 → 대시보드 상시 가동 전에 교체 필요
4. 공유기에 DDNS(`yec123456.iptime.org`)가 설정되어 있음 → 3306 및 대시보드 포트가 외부에 포워딩되지 않았는지 주기적으로 확인
